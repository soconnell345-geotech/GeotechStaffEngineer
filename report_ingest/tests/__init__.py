"""Offline tests for the report-ingest model passes.

Nothing here calls a model or needs a credential: the passes take an engine,
and :class:`report_ingest.tests.fake_engine.FakeEngine` replays canned
replies in its place.
"""
