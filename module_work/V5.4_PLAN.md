# v5.4 plan — owner directives + QC carryovers + creative improvements

**Started 2026-07-06 (owner directive), branch `v5.4`.** Working rules unchanged
(V5.3_PLAN header): additive + default-preserving, validate against published
targets, milestone commits, **NO version bump/tag/publish without owner OK**.

## Workstream D — owner directives (2026-07-06)
- [~] D1 **PDF user manual** — full-package manual covering every problem the
      agent can tackle; auto-generated method/parameter catalog from
      MODULE_REGISTRY/METHOD_INFO + practitioner narrative + validation basis.
      Deliverable: `docs/GeotechStaffEngineer_User_Manual_v5.3.pdf` +
      regenerable builder (`docs/user_manual/build_manual.py`). [agent: manual-gen]
- [~] D2 **No-restart Databricks flow** — `funhouse_agent/runtime_check.py`
      (stale `typing_extensions` detect + in-place reload before langchain
      imports), wired into `funhouse_agent.deep.__init__`; guide/HANDOFF
      updated; cluster-scoped-library alternative documented. [agent: pkg-ux]
- [~] D3 **Disclaimers** — DISCLAIMER.md + prominent README/PyPI section +
      one-time first-import stderr notice (marker file, env suppress, never in
      pytest) + `geotech-disclaimer` console script + calc-package basis block.
      NOTE the honest constraint: pip runs no code on wheel install — the PyPI
      page + first-import notice are the legitimate equivalents. [agent: pkg-ux]
- [x] D4 **Module visualizations** — gallery demonstrating what the custom
      modules do: slope critical-surface search (all surfaces + diagnostics),
      rapid-drawdown 3-stage walkthrough, Newmark integration trace, GLE slice
      forces/thrust line, fem2d SRM shear-strain + displacement fields,
      monolithic consolidation p(z,t) animation, reliability distributions
      (β/Pf), MSE LRFD CDR summary, p-y curve families. Use the existing
      calc_package/plotly viewers; output one self-contained HTML gallery +
      PNG stills; owner-facing, practitioner voice.
      **DONE (2026-07-06, commit 4bb98a8).** 12 exhibits in `docs/gallery/`
      (index.html + 20 PNGs + 2 plotly interactive viewers + regenerable
      build_gallery.py). Every exhibit is a live run of shipped code on a
      published benchmark (F&K-1977, Slide2 #95/#96, Micropile SP2, Griffiths-
      Lane/B6, Itasca V-023, GEC-11 E4, GEC-6, reliability engine-agreement).
      Read-only module usage; no analysis code changed. Full clean build ~60 s
      (fem2d SRM ~47 s dominates); light/dark aware, no horizontal overflow.
- [x] D5 **LLM-ready drawing digitization ("Drawing IR")** — deterministic
      extractor owns geometry; LLM owns semantics. A unified JSON intermediate
      representation for a drawing page: every line/polyline/arc/circle with
      coordinates (model units after scale calibration), text items with
      location/rotation/size, hatches, provenance + confidence per entity.
      Query interface for agents (entities_in_bbox, lines_by_angle,
      text_near(entity), polylines_longer_than, layer/color groups) so the
      LLM requests *slices* instead of interpreting pixels. Sources: DXF
      (native), PDF vector (pdf_import), raster (NEW leg: opencv contour/line
      tracing → IR with lower confidence). Wire into geo_project ingestion
      with provenance quarantine. Validate on the bundled samples + a scanned
      raster fixture.
- [x] D6 **Narrow reviewer agents — seismic first** — two thin surfaces on one
      playbook: (a) `.claude/agents/seismic-reviewer.md` (version-controlled
      Claude Code agent: scope = seismic_geotech, liquefaction, M-O, Newmark,
      site class, pystrata/opensees/seismic_signals; reviews calcs against
      GEC/FEMA/UFC via reference adapters; checklist: units, kh/kv conventions,
      MSF/rd/Ksigma chains, site-class boundary cases, applicability limits);
      (b) funhouse scoped sub-agent (`GeotechAgent(allowed_agents=SEISMIC_SET)`
      + seismic references) exposed so the owner can chat with it independently
      in Funhouse. Pattern: consult_references / geotech-module-specialist.
      Later family members: foundations, earth-retention, slope/FEM.

## Workstream E — QC carryovers (from V5.3_PLAN + adversarial review)
- [x] E1 **B2a-search**: rapid-drawdown critical-surface search wrapper
      (drawdown strengths inside the circular/noncircular search) → validate
      #98 Walter Bouldin (Corps 0.931 / DWW 1.039, search minima).
      **DONE (2026-07-07, 63b8d29).** `search_rapid_drawdown` via additive
      `fos_fn` hook; #98 minima 0.837/0.938 (~10% low, geometry-limited —
      recovered raster section; ordering correct); wrapper exactness proven on
      the exact #95/#96 section. V-041 + VALIDATION.md §B7.
- [x] E2 **#96 Kc interpolation for c'=0** (V-038 1.273 vs 1.443): revisit the
      anisotropic-consolidation strength gain when the drained envelope falls
      below R at low σ'_fc (Duncan-Wright-Brandon Ch.9 refinement).
      **DONE (2026-07-07, c26f7c6) — re-diagnosed.** Kc interpolation is sound
      (~1.45 in isolation); the deficit was the stage-3 Fellenius effective-
      normal estimate (inconsistent with stage 1's rigorous GLE normal).
      Opt-in `stage3_effective_normal='gle'` → 1.306/1.370 vs 1.443 (~5%
      residual). Specialist judges the Fellenius default an outright bug;
      **OWNER DECIDED 2026-07-08: default STAYS 'fellenius'** — owner recalls
      a substantive reason for the cruder stage-3 estimate (checking grad-
      school notes); `'gle'` remains opt-in. Do NOT flip without a new owner
      call.
- [x] E3 **Pore-pressure grid (TIN u(x,z)) input** → flips ACADS 5 (V-029).
      **DONE (2026-07-07, bf816a4).** `pore_pressure_points` TIN input,
      machine-precision-rigorous tests; published 1.498 stays unpinned
      (Fig 10.2 grid unrecoverable — capability built, honest demo only).
- [x] E4 **Exit-side tension crack + mass-truncation crack model** (V-026).
      **DONE (2026-07-07, 9e9550f).** `tension_crack_side` + `tension_crack_model`;
      truncation model RESOLVES the V-026 residual (<0.1% vs published);
      exit-side kills the mirror workaround. Defaults byte-identical.
- [x] E5 **Composite/transformed-section EI helper** (lateral_pile; honest
      V-017 flip). Larger moment-curvature EI = owner-call.
      **DONE (2026-07-07, c3fd651).** `composite_section_ei` (filled pipe /
      cased+grouted+bars / RC circ+rect; ACI 318 Ec; uncracked basis) +
      `Pile.from_composite_section()` + adapter method; V-017 flipped.
- [x] E6 **Pile passive-force option** (#54 +2.5%) + StabilizingPile polish.
      **DONE (2026-07-07, 351efc9).** `support_convention='active'|'passive'`
      (Method A/B); V-040 with-pile residual 1.223 (+2.5%) → 1.203 (+0.8%)
      vs published 1.193. Circular moment methods only (GLE unaffected by
      construction — documented). Adapter exposed. No other genuine polish
      found (honest no-op).
- [x] E7 **B2e SRM mesh-consistency follow-up** (fem2d↔LE cross-check tail).
      **DONE (2026-07-07, 43e6deb).** `srm_mesh_refinement_study` +
      `MeshRefinementResult` (convergence table, Richardson extrapolation when
      monotone). GL99 Ex1 banded 1.34–1.38 vs published 1.4 (noisy, honest
      no-Richardson); Bishop cross-check monotone → Richardson 1.376 (p≈2.8).
      Constant-stiffness low-c'/φ failure-detection mesh dependence is a
      SEPARATE tracked follow-up (UPGRADE_PLAN.md), explicitly untouched.
      fem2d gate 379 passed.
- [x] E8 **Multiple surcharge zones** (slope_stability; ACADS-4 note).
      **DONE (2026-07-07, 138f819).** `SlopeGeometry.surcharges` list summed
      via `surcharge_at(x)` (slices/search wired automatically); V-028 with
      both zones: Spencer 0.792→0.788 (~0.4%, seam governs as expected).
      Adapter exposed. Plus e4cf5f3: ALL v5.4 slope capabilities wired into
      the funhouse slope adapter (pore grid, crack side/model, stage-3 normal,
      search_rapid_drawdown method) with offline tests; funhouse gate 869/5.
- [x] E9 **Review-deferred cleanups**: newmark ky bisection slice reuse;
      pdf_import join/snap O(n²)→spatial hash; rapid-drawdown O(n²) slice
      alignment; pdf adapter build_* shared helper; fem2d adapter duplicate
      scheme validation; _lininterp reuse in soe; PA constant dedup; test unit
      constants via geotech_common.units.
      **DONE (2026-07-07, b96b849/4d16e01/62a4218/5af8ee2 + lead PA dedup).**
      All applied items proven bit-identical (fuzz + pinned suites). Two
      HONEST declines, recorded: (a) join_polylines greedy merge left as-is
      (order-dependent; not the hot path; hash risks output change);
      (b) test unit constants NOT swapped to geotech_common.units — its
      psf factor differs from the tests' truncated PSF (numeric change) and
      the public multipliers would be new API; owner call if wanted.
- [~] E10 **Eval refresh**: extend the 71-Q suite with questions exercising the
      v5.3 tools (rapid drawdown, Newmark, infinite slope, stabilizing piles,
      log-spiral Kp, MSE LRFD, monolithic consolidation, pdf geometry tools) +
      answer keys; owner re-runs on 5.3/5.4 with `[deep,full]` (live,
      owner-gated).
      **OFFLINE PART DONE (2026-07-07, 864f179).** 71→96 questions, 25 new
      keys all computed by running the modules + re-verified by offline test
      (test_eval_suite_v54.py); manifest docs/geotech_eval_v54_additions.md.
      Follow-ups: (a) log-spiral Caquot-Kerisel Kp NOT adapter-reachable —
      wiring assigned to slope-qc, then add the ready-keyed question;
      (b) fem2d consolidation/mesh-study questions left for pile-fem-qc to
      key; (c) LIVE re-run on Databricks = owner-gated.
- [~] E11 Reference-layer backlog: ufc_expansive OCR figures (~22); module-
      SELECTION mis-routing aliases (Rankine guessed on the wrong module);
      fem2d footing-SRM convenience method; weekend-QC gec_6/7/12/13
      chapter-text decision.
      **2 of 4 DONE (2026-07-07):** mis-routing redirects (abbe1ad —
      did-you-mean to the right module+method, never auto-executes;
      retaining_walls in-module aliases return a real K) and
      `fem2d.analyze_footing_capacity` (0aea590 — load-ramp to collapse;
      Prandtl Nc 5.12 vs 5.14, −0.4%; adapter `fem2d_footing_capacity`).
      **ufc_expansive figures DONE (2026-07-08, submodule 46f6c60 local — push
      permission-blocked, owner to push or OK):** the "~22" was wrong — LoF
      says 42 total, 32 were catalogued, the 10 Appendix C figures added with
      exact pages (all 42 now page_estimated=0, 0 unrecoverable; vision OCR,
      pytesseract not installed). Submodule suite 3707 passed. Parent pointer
      bumped on v5.4.
      REMAINING: gec_6/7/12/13 chapter-text decision (owner/weekend-QC).
      Eval suite now **100** questions (f0a7948: CON-1 U=0.599, FTG-1 Nc=5.12,
      MRS-1 finest-mesh FOS 1.531).

## Workstream F — creative improvements (Fable proposals, priority order)
- [x] F1 **Depth-varying su strength law + correlated-gradient random
      variable** in the probabilistic engine — closes the V-030 input-COV
      FOSM scope gap (LASH: su = a + b·z with Var[a], Var[b], ρ). Small,
      well-anchored (Duncan 2000 published Pf 18%).
      **DONE (2026-07-07, 5d8f7a6).** `linear_su` law (coherent correlated
      (a,b) pair; FOSM cross-term + bivariate-normal MC; no-law path
      byte-identical) + `gamma_sat` scalar variable (buoyancy-amplified:
      ~42% of Var[F] at COV 0.033). Duncan anchor honest: stated Table-29.2
      inputs → Pf ~13% (Slide2 per-method band); his COV_F=0.16 recovered →
      Pf 18.2% with full-profile su COV — both reported, not tuned. FOSM≈MC.
      V-030 upgraded PASS (det + reliability + input-COV FOSM).
- [x] F2 **More Slide2 catalog problems** (the manual has 111; we used ~15):
      probabilistic #33-36 (reliability-index anchors for FOSM/MC), reinforced
      #30-32/#39/#50 (geosynthetics), tiebacks #85/#86, Ito-Matsui #106 full
      FOS (needs E1-style pile+search), submerged #70. Cheap validation mass.
      **CLOSED FOR 5.4.0 (2026-07-08, f2d16c7/d462cb7/4be066a).** V-042 (#36
      probabilistic, RI 2.30 vs 2.336) PASS; V-043 (#39 reinforced)
      PASS/CONVENTION; V-044 (#70 submerged, water-level independence) PASS;
      +`ru` probabilistic variable; F2c search-admissibility guard (one-sided
      below-base plunge rejection, default-ON, ZERO pinned results moved) —
      #85/#86 baselines physical (0.72→0.912, 0.55→1.549), with-support =
      CONVENTION; #106/#51 N/A(source); #35 SKIPPED(source); #50/#30-32
      SKIPPED(complexity). **DEFERRED post-release: correlated c'-φ'
      scalar-pair feature + #33/#34** (next train).
- [ ] F3 **Bray & Travasarou (2007) seismic displacement correlation** as a
      Newmark cross-check (published coefficients, unit-testable) + kmax/
      ky·Ts inputs; complements Jibson.
- [~] F4 **~~Corps of Engineers #1/#2 +~~ Lowe-Karafiath method** — the Slide2
      drawdown tables publish these columns; adds cross-validation breadth to
      the method comparison table.
      **SCOPE CORRECTED (2026-07-11, research scout vs EM 1110-2-1902 App G):**
      "Corps #1/#2" is a menu-name collision — in Slide2/GeoStudio those label
      INTERSLICE FORCE FUNCTIONS, not drawdown methods (our GLE/Spencer family
      already generalizes those). The authoritative drawdown set is exactly:
      Corps-1970 2-stage (BUILT, min(R,S) at pre-drawdown σ'_fc — V-037),
      DWW 3-stage (BUILT), and **Lowe-Karafiath = DWW minus stage 3** (the
      remaining build). New anchors: #97 Pilarcitos (0.823/1.047/1.043),
      #98 Bouldin LK 1.075, #99 pumped-storage (1.345/1.620/1.534).
- [x] F5 **Local factor-of-safety heatmap** (IMPROVEMENT_PLAN Ph-6 idea):
      fem2d stress field → pointwise mobilized/available strength contour;
      pairs with D4 visualizations.
      **DONE (2026-07-08, 3dd877f).** `local_fos_field` (reciprocal of PLAXIS
      relative shear stress; per-element+nodal, cap 10) +
      `analyze_slope_srm(compute_local_fos=)` + plot_local_fos + adapter
      `fem2d_local_fos` + gallery Exhibit 13. GL99: min-local/global 0.97
      (critical) / 0.93 (stable) — the tan(phi) reduction nuance and boundary
      stress concentrations documented honestly. fem2d 395 / funhouse 914.
- [ ] F6 **Anisotropic undrained strength** (su vs base inclination), common
      in soft-clay practice; SSAP-class feature.
- [x] F7 **Slope calc-package report** — one-call HTML/PDF calc package for a
      slope run (geometry, searches + rejection diagnostics, method table,
      thrust line, probabilistic annex); mirrors the existing calc packages.
      **DONE (2026-07-11, 90b1709).** NEW method `slope_report_package`
      (existing slope_stability_package byte-identical): runs the search,
      re-analyzes critical w/ compare_methods, FOSM/MC annex, rejection
      diagnostics in the search story. Validated on V-028 (Spencer 0.791 vs
      referee 0.78; 461/700 rejections rendered); PDF fitz-verified (7 pp).
- [x] F8 **Reviewer-agent family rollout** beyond seismic (foundations,
      earth-retention, slope/FEM) once D6 pattern proves out.
      **DONE (2026-07-08, cfc1a73).** 3 reviewers × 6 pieces each on the D6
      pattern (checklists single-sourced, .md pastes enforced verbatim by
      test; scopes verified vs MODULE_REGISTRY). Judgment calls: gec5 is
      characterization NOT soil-nail → slope-fem uses gec7+gec11;
      seismic_geotech split M-O (earth-retention) vs rest (seismic reviewer),
      stated in both checklists. funhouse 965 passed.

## Next train — STARTED 2026-07-10 (owner "go ahead"; 5.4.1 shipped same day)
- slope-qc (owns slope_stability internals): (1) correlated c'-φ' scalar-pair
  + Slide2 #33/#34 (#35 SKIPPED(source)); (2) F3 Bray-Travasarou 2007;
  (3) F4 CoE #1/#2 + Lowe-Karafiath; (4) F6 anisotropic su. Sequential,
  report per item.
- pile-fem-qc: F7 slope calc-package report (calc_package/** +
  slope_stability/calc_steps.py only + adapter + webapp cards if needed).
- TinyApp: owner SUBMITTED the hosting form 2026-07-10 (~1 week for
  feedback); webapp/ is the submission artifact. Fold platform feedback in
  when it arrives.
- **OWNER CLOSE-OUT DIRECTIVE (2026-07-10, execute when this train wraps;
  owner specified the LEAD — Fable — performs it directly, NOT delegated to
  the build agents):**
  (1) large-scale housekeeping — untracked/stale file sweep in worktree +
  main checkout, plan/task hygiene; (2) full documentation update — HANDOFF,
  CLAUDE.md, guides, plan close-out, memory; (3) QC pass — adversarial
  review of the train's diff (v5.3-style multi-angle) AND a rigorous QC of
  the F7 slope calc report deliverable (generate on validated fixtures,
  verify every section + PDF), then a full-suite gate.

## Next train (post-5.4.0) candidates
- Correlated c'-φ' scalar-pair variables + Slide2 #33/#34 (slope-qc, held ready).
- F3 Bray-Travasarou 2007; F4 CoE #1/#2 + Lowe-Karafiath; F6 anisotropic su;
  F7 slope calc-package report.
- Deep prompt real-file routing fix (c64e57a, already on branch) — from the
  live Bamako BTL session: agent never tried analyze_pdf_page on a real /tmp
  PDF after read_file failed.
- **5.4.1 PATCH IN FLIGHT (2026-07-10, owner-prioritized, pile-fem-qc):**
  `read_pdf_text` (PyMuPDF text layer, page-ranged, attachment-or-real-path,
  scanned-page flags) + real-path fallback for analyze_pdf_page/analyze_image
  + corrected deep-prompt file-routing bullet + FileUpload attachment widget
  in the notebook chats. From the live Bamako BTL session (agent couldn't
  read a staged /tmp PDF; attachments-dict was the only working mechanism).
  Release = owner call.
- **Databricks Apps web chat (owner greenlit scoping, 2026-07-10):** a
  workspace-hosted chat app (drag-drop uploads, persistent sessions,
  URL-shareable with workspace auth) over the existing engine-agnostic deep
  agent. NEXT-TRAIN headline item: design + clickable prototype; check
  whether State's workspace has Apps enabled (fallback: cluster-hosted app
  behind the driver proxy).

## Log
- 2026-07-06: plan written. D1 (manual-gen) + D2/D3 (pkg-ux) dispatched;
  D4-D6 queued next wave; E/F prioritized for after the owner's Funhouse
  feedback on 5.3.
- 2026-07-06: **D6 DONE — seismic reviewer** (first narrow reviewer). Two thin
  surfaces over one shared playbook: (a) `.claude/agents/seismic-reviewer.md`
  (version-controlled Claude Code agent, tools Read/Grep/Glob/Bash); (b) Funhouse
  `funhouse_agent.make_seismic_reviewer(engine)` + `make_seismic_reviewer_deep(model)`
  (deep variant — build_deep_agent already took `allowed_agents`, added a small
  additive `extra_system_prompt` hook; GeotechAgent gained a matching
  `system_prompt_extra`). Scope sets in `dispatch.py`: `SEISMIC_MODULES` (9:
  seismic_geotech, liquefaction, liquepy, slope_stability, opensees, pystrata,
  seismic_signals, hvsrpy, fem2d) and `SEISMIC_REFERENCES` (6+2: fema_p2082, dm7,
  gec5, gec7, gec11 + reference_db/figure_db — picked from the references' ACTUAL
  seismic chapter-text content; the 3 UFC docs are NOT seismic, excluded). Shared
  checklist in `funhouse_agent/review_checklists.py` (SEISMIC_CHECKLIST +
  SEISMIC_REVIEWER_PREAMBLE) imported by the factory and pasted (with sync pointer)
  into the .md. reference_mode="off" on both (seismic refs are directly in scope).
  Tests: `funhouse_agent/tests/test_reviewers.py` (22, incl. deep-variant build).
  Gate: `pytest funhouse_agent/ -q` = 1081 passed, 6 skipped. Docs: "Narrow
  reviewer agents" section in `docs/funhouse_agent_guide.md`. Family rollout
  (foundations / earth-retention / slope-FEM) is F8, once this proves out.
- 2026-07-06: **D5 DONE — Drawing IR** (new top-level module `drawing_ir/`).
  Deterministic extractor owns coordinates; LLM owns semantics via structured
  slice queries. Schema (`results.py`): `DrawingIR` (page meta: size/units/
  coordinate_space/scale+provenance/origin) + entities Line/Polyline/Arc/Circle/
  TextItem/Region, each with id/layer/color/style/source/confidence/bbox; lossless
  JSON round-trip. Ingest (`ingest.py`): from_dxf (ezdxf, conf 1.0, $INSUNITS→SI m,
  layers/colors, ellipse/spline flattened, best-effort HATCH), from_pdf_vector
  (reuses pdf_import extract_colored_paths/discover_pdf_content + scale module;
  page-space default with scale CANDIDATES as proposals, model-space on explicit
  scale/two-point calibration, conf 1.0), from_raster (NEW OpenCV leg: Hough lines
  0.6 / Hough circles 0.5 / contour polylines 0.5 / OCR text 0.4 best-effort via
  pytesseract else positions-only-with-warning). Cross-import ruling: `ingest.py`
  legally imports dxf_import/pdf_import — same I/O-layer pattern as
  geo_project/ingest.py (the no-cross-import rule targets the 30 analysis modules);
  results.py/queries.py kept pure-schema. Queries (`queries.py`, the LLM surface):
  entities_in_bbox, nearest_entity (true point-to-geometry distance), lines_by_angle/
  horizontal_lines/vertical_lines (over line+polyline segments), polylines_longer_than,
  text_items(pattern)/text_near(entity,radius), entities_on_layer/entities_by_color,
  candidate_ground_surface (PROPOSAL only), summary_stats, get_entities — all return
  compact refs, never full coord dumps. Adapter
  (`funhouse_agent/adapters/drawing_ir_adapter.py` + registry): digitize_drawing
  (path→handle+stats, IR cached server-side by handle, full IR never returned),
  query_drawing (handle+query allowed_values+params), get_entities (handle+ids→exact
  coords); registered in MODULE_REGISTRY → auto in ANALYSIS_MODULES (primary agent
  directly callable). Installed `opencv-python-headless`. Tests: 61 in `drawing_ir/`
  (schema round-trip, per-leg ingest, query correctness w/ known coords) + 19 adapter
  = 80 new; gate `pytest drawing_ir/ funhouse_agent/tests/ pdf_import/ dxf_import/ -q`
  = 1137 passed, 5 skipped (existing untouched). DESIGN.md + CLAUDE.md inventory row
  added. Follow-up (deferred): wire a confirmed IR into geo_project ingestion with
  provenance quarantine (schema already carries the provenance+confidence it keys on).
- 2026-07-07: **ALL OWNER DIRECTIVES D1–D6 DONE** (D4 gallery 4bb98a8/c96cef3;
  D5 drawing_ir c06ecde; D6 seismic reviewer 0d44260/5ebe0b2/9b4cba9; D1 manual
  5248a48/6e59f53; D2/D3 pkg-ux 6e6f795/9172893). Final full-suite gate on the
  branch: **8279 passed / 48 skipped**. Branch `v5.4` pushed to origin; NOT
  merged to master (owner validating 5.3 in Funhouse; release call is theirs).
  Deliverables sent to owner: manual PDF + gallery zip. Docs refreshed for a
  cross-session pickup (HANDOFF §1/§2/§3/§6, CLAUDE.md v5.4 status block).
  Next work = E1–E11 / F1–F8 above, or owner feedback items.
