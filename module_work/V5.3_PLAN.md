# v5.3 plan — Batch 2 coverage + slope_stability round 2 + pdf_import round 2

**Started 2026-07-05 (owner directive).** Branch `v5.3-coverage` (worktree
`.claude/worktrees/v5.1-todos`), from the 5.2.0 release commit. Same working
rules as v5.2 (`module_work/V5.2_COVERAGE.md`): additive + default-preserving,
validate against published targets (don't tune), stagger agents, milestone
commits, **NO version bump/tag/publish without owner OK**.

## Workstream A — Batch 2 carryover (exact targets in validation_examples/INVENTORY.md)
- [x] A1 drilled_shaft rational GEC-10 chains — DONE (commit `32c25de`):
      β=0.413 vs 0.41 (+0.7%), α=0.475 vs 0.47 (+1.1%), RSN both −0.1%;
      high-level path, beta_method/alpha_method/su_test_type selectors,
      12 new tests; V-006/007 PASS. 168 drilled_shaft+validation tests green.
- [x] A2 MSE LRFD external-stability CDR path — V-009 DONE:
      new `retaining_walls.mse.check_external_stability_lrfd` computes the full
      AASHTO/GEC-11 CDR set (sliding / eccentricity / bearing) under Strength I
      max+min + Service I with the LL-exclusion + min-vertical "critical"
      bookkeeping built in; also reachable via `analyze_mse_wall(lrfd_external=True)`
      → `result.external_lrfd` and the funhouse `mse_lrfd_external_stability`
      adapter method. High-level path reproduces every E4 CDR (sliding
      1.85/2.08/1.37, eL 2.87/3.87 ft < L/4, bearing σv 6.70 ksf / CDR 1.57,
      Service 4.66 ksf); load/resistance factors are documented params w/ GEC-11
      defaults. Default ASD path byte-identical. V-009 rows flipped to PASS. 2 new
      validation tests; retaining_walls (93) + validation + funhouse suites green.
- [x] A3 soe basal heave w/ sidewall shear (FS=1.54) + FHWA apparent-pressure
      anchored wall + log-spiral Kp — V-013/014 DONE (commit `e7e16f5`). Three
      additive, default-preserving capabilities:
      (1) `soe.check_basal_heave_caltrans` — Caltrans force balance WITH the
      sidewall-shear term S=cu·H (FS=1.55 vs pub 1.54; 1.538 with chart Nc=7.6).
      (2) `caquot_kerisel_Kp` (sheet_pile + soe) — Caquot-Kerisel log-spiral
      passive; φ=30, δ/φ=0.5 → Kp=4.70 (pub 4.7); `pressure_method="log_spiral"`.
      (3) `soe.fhwa_apparent_pressure_anchored_wall` — native FHWA tributary
      method; V-016 (2-anchor) pe/TH1/TH2/R/DL native, V-013 single-anchor
      envelope (σ_a=934.4, PT=15,573, T1U=6,229) native. Adapters + DESIGN.md +
      4 new validation tests; soe(111)+sheet_pile(36) unchanged; funhouse 792.
      **A3 FOLLOW-UP (residual, not done):** the V-013 single-anchor embedment
      D=6.09 ft needs an APPARENT-diagram free-earth-support solver (trapezoidal
      1.3× active instead of the triangular one). The log-spiral passive is
      available and drops the classical-FES D from ~10.8 → ~7.2 ft, but
      reproducing 6.09 exactly (and the single-anchor total anchor force TH/T)
      needs that solver.
- [x] A4 lateral_pile full Reese (1974) sand p-y — V-017 DONE (additive; NO
      deflection flip — root cause is EI, not the p-y). Added
      `SandReese(construction="simplified"(default)|"reese1974")`: the full
      four-segment Reese (1974) curve (linear → parabola → straight m-segment →
      plateau) with the A/B chart coefficients (`_reese_A`/`_reese_B`, cited to
      Reese & Van Impe 2001 / COM624P; asymptotes A_s=0.88/A_c=0.53/B_s=0.50/
      B_c=0.55). Adapter `construction` allowed_values; DESIGN.md; construction
      tests. Default byte-identical (109 lateral_pile tests unchanged).
      **KEY FINDING:** the full construction gives 5.02 mm for V-017 — SOFTER than
      the simplified 3.92 mm (m-point pm=B·pu with B→0.5 is genuinely soft), so it
      does NOT close the gap. Root-caused the V-017 deflection gap to the section
      BENDING STIFFNESS: the published LPILE run used nonlinear/composite EI
      (extract "Computed … Using Nonlinear EI") vs the test's casing-only elastic
      EI; EI×1.3 → exactly 3.28 mm ≈ 3.3. k (24430/16287) is the source's
      documented value. V-017 deflection stays CONVENTION (note rewritten); Mmax
      PASS. A/B asymptotes NOT tuned.
      **A4 FOLLOW-UPS (flag, not built):** (i) SMALL — an uncracked transformed-
      section composite-EI helper (casing+grout+bar from the SP-2 documented
      geometry, if the micropile manual gives it); that computed EI is deterministic
      fidelity (not tuning) and would likely flip V-017 deflection to PASS honestly.
      (ii) LARGER, owner-call — a full nonlinear moment-curvature EI capability for
      lateral_pile.
- [x] A5 fem2d monolithic u-p consolidation (undrained transient) — V-023 [hardest, last]
      → DONE, commit d1e27a7. Additive `solve_consolidation(scheme="monolithic", theta=…)`
      with a Taylor-Hood T6/T3 pairing (LBB-stable, no stabilization term). Undrained
      p0 = 83.92 kPa (exact vs formula-consistent 0.839e5; Itasca 0.981e5 needs M≈10×,
      tuned to neither); Terzaghi decay <1% (θ=0.5) / ≤3.1% (θ=1) inside the FLAC <5%
      envelope; drained end-state 2.596 mm. Default staggered byte-identical; 369 fem2d
      tests unchanged + 3 new. V-023 undrained rows flipped to PASS in RESULTS.md.

## Workstream B — slope_stability round 2
Source: Slide2 Slope Stability Verification Manual (public;
https://static.rocscience.cloud/assets/verification-and-theory/Slide2/Slide_SlopeStabilityVerification.pdf
— 111 problems w/ published/referee answers). Raw text of the 21 selected
problems: `module_work/slope_v53/slide2_selected_problems.txt` (extracted
2026-07-05; manual page refs inside). PDF kept out of the repo (21 MB).

**B1 — new validation problems the module can already run** (add to
validation_examples INVENTORY/RESULTS + tests, Phase-E style):
IN PROGRESS (agent `slope-valid`, 2026-07-05). Geometry figures were missing from
the text extract; recovered from the manual's own labeled figures via a
PDF-render research pass. Status per problem:
- [x] #2 ACADS 1(b) tension crack w/ water (referee 1.65) — **V-026, CONVENTION**
      (8 tests). Module's water-filled crack ~6-8% more conservative than Slide2
      (keeps cracked wedge as driving + full hydrostatic thrust); dry crack matches
      Slide2 water crack within 3%. Geometry MIRRORED (crack is entry-side only).
      B2: exit-side crack + mass-truncation crack model.
- [x] #4 ACADS 1(d) 3-layer + seismic k=0.15 — **V-027, N/A(geometry discrepancy)**.
      Even with the team-lead's corrected geometry the module gives static Bishop 1.02
      / seismic 0.77 vs published static 1.405 / seismic 1.016. Soil assignment VERIFIED
      correct; a full soil-interpretation sweep found none reaching 1.405 (target =
      effective φ≈29, between homogeneous φ23=1.305 and φ38=1.562). Seismic/static ratio
      0.723 matches the homogeneous cases, and the seismic engine is validated by #62 —
      so the published critical must avoid the weak Soil#3 toe (deeper than the figure
      read). Not forced.
- [x] #9 ACADS 4 weak layer + piezo — **V-028, PASS (v5.3 B2, after the SS-6 search
      fix)**. The degenerate ~0.05-0.18 surfaces were a solver-non-convergence artifact
      in the noncircular search; with the SS-6 fix the `weak_layer` search reliably finds
      the seam-following critical at Spencer 0.792 (+1.5% vs referee 0.78), and
      `noncircular_de` reaches ~0.69 (Slide2 optimized 0.707). See B2 search-robustness
      below. (Single-surcharge-zone remains a minor B2 gap.)
- [x] #10 ACADS 5 pore-pressure grid + ponded (referee 1.53) — **V-029, N/A(scope)**:
      no pore-pressure-GRID (TIN u(x,z)) input in the module. B2 capability.
- [x] #29 Duncan (2000) LASH underwater slope (FS 1.17 / Pf 18%) — **V-030, PASS**
      (det. FOS Spencer 1.190 vs 1.157 +2.9% on Duncan's noncircular surface;
      Taylor-series reliability F=1.17->Pf 18%). Input-COV FOSM propagation is
      N/A-scope (needs depth-varying-su law w/ one correlated gradient var -> B2).
- [x] #57 Pockoski & Duncan test slope 3 — **V-031, PASS** (Bishop/Spencer <0.6%).
- [x] #61 Baker (2003) — **V-032, PASS** (Spencer 1.365 vs 1.366; power-curve
      variant out of scope).
- [x] #62 Loukidis (2003) ex1 seismic — **V-033, PASS** (Spencer 1.005 at kc=0.432;
      validates the pseudo-static seismic engine on a clean labeled geometry).
- [~] #63 Loukidis (2003) ex2 3-layer seismic — geometry-limited (UNLABELED figure,
      ~+-1.5 m; seismic ~0.82 vs 1.0 target — same imprecise-3-layer-geometry issue
      as #4; seismic engine validated by #62).
- [x] #79/#81 infinite-slope cases — **V-035/V-036, PASS** (B2c `infinite_slope_fos`;
      1.443 / 1.155 exact).

**B2 round 1 (DONE 2026-07-06, agent `slope-valid`):**
- [x] **SS-6 weak-layer / noncircular search degenerate-surface fix** (`search.py`).
      Root cause: on jagged random polylines the rigorous GLE fails to converge (or
      converges to garbage) and the legacy Spencer fallback returns a spurious low FOS
      (~0.05-0.2) that the search picked as critical (ACADS 4). Fix in `_compute_fos`:
      (1) geometric admissibility (min slices, min span); (2) REQUIRE GLE convergence for
      noncircular trials (`_rigorous_noncircular_fos` → None otherwise, no legacy fallback);
      (3) a low-FOS jaggedness gate (`_is_jagged` + `_LOW_FOS_JAGGED_GATE`) — a low FOS on a
      non-concave surface is a solver artifact. Scoped to low-FOS surfaces so exploration is
      untouched; circular path byte-identical. Re-validated #9 (V-028 → PASS). Slope suite
      green (test_b2_round1.py adds 9 tests). Commit: <search-fix>.
- [x] **B2c Infinite slope method** — `analysis.infinite_slope_fos` (dry / seepage-parallel
      / ru / c-φ closed form) + `InfiniteSlopeResult` + adapter `infinite_slope_fos` +
      DESIGN.md. Validated V-035/V-036 (#79/#81). Commit: <infinite-slope>.

**B2 — remaining capability builds surfaced by the above:**
- [x] B2a **Rapid drawdown (Duncan/USACE)** — DONE + UPGRADE PASS. stub CLOSED.
      New `slope_stability/rapid_drawdown.py` (`rapid_drawdown_fos`, method
      'corps_2stage'|'duncan_3stage') + R-envelope layer fields (R_c/R_phi) +
      adapter + DESIGN theory + `RapidDrawdownResult`. Wired to the GLE engine
      (stage-1 sigma'_fc/tau_fc -> stage-2 undrained envelope / Kc interpolation ->
      stage-3 drained substitution -> drawdown-pool FOS).
      **Upgrade pass** (exact Fig 95.1 two-segment face calibrated to the published
      circle + new `stage1_phreatic_points` steady-seepage option, default unchanged):
      * V-037 (#95 Corps 2-stage) → **PASS (steady-seepage stage-1)**: flat bound
        1.21, declined-phreatic 1.34 vs published 1.347 (~0.6%). `min` R/effective
        composite confirmed correct (pure R → 1.42). Exact geometry alone ≈1.21
        (did NOT close the gap) — residual WAS the stage-1 flow net.
      * V-038 (#96 DWW 3-stage) → **CONVENTION**: flat 1.235, seepage 1.273; ordering
        holds but ~12% below 1.443 at the same phreatic. Isolated to the Kc
        anisotropic interpolation for c'=0 (drained envelope below R at low σ'_fc);
        documented follow-up, NOT geometry/seepage. Stage-3 left per DWB-2014 Ch.9.
      * #98 (5-material Walter Bouldin) → **DEFERRED (search-limited)**: geometry +
        R-data recovered/recorded (INVENTORY) but the published values are SEARCH
        minima; needs a rapid-drawdown search wrapper (single-surface API) →
        candidate B2a-search. #102 out of scope (transient-seepage FEM).
      7 module + 4 validation tests; full slope+validation+adapter suite green.
- [x] B2b **Newmark sliding-block displacement** — DONE. `slope_stability/newmark.py`:
      `yield_acceleration` (ky by bisection of the pseudo-static FOS→1, ay=ky·g),
      `newmark_displacement` (rigid-block double integration, downslope-only /
      both-polarity, trapezoidal), `newmark_jibson2007` (Jibson-2007 Eq.6 regression,
      coeffs 0.215/2.341/−1.438, σ0.51). Exported + 3 adapter methods with schema.
      V-039 → **PASS (integrator + Jibson) / #104 documented**: #104's Tutorial-28
      acceleration record + geometry are unpublished, so the integrator is validated
      against the exact rectangular-pulse closed form and Jibson against its published
      equation; #104 four-scenario structure reproduced qualitatively (FS 1.359/0.978
      ⇒ FS=1 at k≈0.141 ≈ Ky 0.139; Jibson brackets 5.042 cm at PGA 0.35–0.60g).
      8 module + 3 validation + 4 adapter tests.
- [x] B2c **Infinite slope method** — closed form incl. seepage-parallel case;
      validate vs #79/#81.
- [x] B2d **Stabilizing-pile resistance (Ito & Matsui)** — DONE.
      `reinforcement.StabilizingPile` on `geom.stabilizing_piles`, resolved via
      `compute_reinforcement_forces` (active convention) at the slip crossing. Two
      modes: specified `shear_capacity` (kN/pile → /spacing) OR `ito_matsui=True` +
      `diameter` → `ito_matsui_lateral_force`/`ito_matsui_pressure` (c-φ plastic-
      deformation force + φ=0 cohesive limit). Adapter `stabilizing_piles` geometry
      param + schema. V-040 (#54 Yamagami) → **CONVENTION**: no-pile 1.114 vs 1.102
      (+1.1%), with-pile 1.223 vs 1.193 (+2.5%); pile-force integration exact (φ=0
      closed form). Residual = active-vs-passive support convention + figure-read pile
      location + single-pile search subtlety. Ito-Matsui FORMULA unit-tested for the
      #106 spacing trend (#106 cross-section not in the manual). 5 module + 4 validation
      + 3 adapter tests.
- [ ] B2e SRM mesh-consistency follow-up (memory note; fem2d cross-check tail)
Adapter exposure + DESIGN.md updates for each; funhouse adapter allowed_values.

**Remaining v5.3 slope follow-ups (flagged, NOT built):**
- **B2a-search** — a rapid-drawdown search wrapper (evaluate the drawdown strength
  over a circle/noncircular grid, take the min) to reproduce Slide2 #98's search-minima
  (Walter Bouldin 5-material). Geometry + R-data recorded in INVENTORY.
- **#96 Kc follow-up** — the Duncan-Wright-Wong 3-stage under-predicts ~12% for c'=0
  soils (drained envelope below the R envelope at low σ'_fc); needs a τ_ff-vs-σ'_fc
  envelope-crossing treatment per Duncan-Wright-Wong (1990). See DESIGN.md / RESULTS V-038.
- **B2e SRM mesh-consistency** — fem2d strength-reduction cross-check tail (memory note).
- **Pile passive-force option** — the active convention over-predicts the pile benefit
  ~2.5% on #54; a passive (resisting-side) application option would tighten it.

## Workstream C — pdf_import round 2 (geometry from PDFs/images)
Current: vector (PyMuPDF, PDF-only, color→role mapping, manual `scale` float) +
vision (`image_fn`, accepts PDF page or raster image, manual scale). Gaps →
build order:
- [x] C1 **Scale calibration**: two-point calibrate helper
      (`calibrate_scale(p1,p2,distance_m)`); auto-parse "SCALE 1:N" text +
      dimension-label detection to propose scale (returned as candidate w/
      provenance, never silently applied).
- [x] C2 **Label→region association**: attach text annotations (CLAY/FILL/GWT)
      to nearest/enclosing polyline region → auto role_mapping proposal.
- [x] C3 **Geometry cleanup**: endpoint snapping, collinear merge, polyline
      joining, dedupe (tolerance-param'd) before build_slope_geometry.
- [x] C4 **Vision grid overlay**: render page w/ labeled coordinate grid before
      the vision call (read-off accuracy); plumb into extract_geometry_vision.
- [x] C5 **Vision↔vector cross-check**: compare the two extractions, produce a
      discrepancy report (geo_project provenance-quarantine spirit).
Raster edge-tracing (opencv) DEFERRED — vision path covers raster input.

## Adversarial review (2026-07-06, Fable-orchestrated: 8 finder angles → verification → fixes)

**Confirmed + FIXED (commits d7d70aa, 8740ec5, f5cf1dc, 5781c17, c4ee2be, e2f0723, 828483a):**
- log-spiral `caquot_kerisel_Kp` δ=0 clamped to the δ/φ=0.40 column → Kp 4.32 vs
  Rankine 3.00 (UNCONSERVATIVE, hit the wall-path default δ=0) — now
  Rankine-anchored interpolation below δ/φ=0.40, both copies + a parity test.
- basal-heave Caltrans F_dr≤0 returned FOS=∞/passes=True (masked cases where
  Bjerrum-Eide FAILS at 1.0) — now passes=False + applicability note.
- Reese-1974 cyclic A-coefficient mis-digitized (A<B from z/b≈1.4 silently
  degenerated the 4-segment curve) — corrected A≥B + warn on residual fallback.
- Newmark: abs()-rectified record was the only mode and mis-cited Jibson —
  now `polarity="downslope"` (standard, default) / `"rectified"` (conservative).
- monolithic `degree_of_consolidation` ≡ 1.0 (self-ratio) — now
  pore-pressure-dissipation based; + u-p factorization reused across steps.
- rapid-drawdown stage-3 non-convergence silently ignored (dead ternary) — now
  surfaced (`stage*_converged` + warnings).
- search span floor was 15% of FULL model width (rejected legitimate localized
  failures in wide models) — re-anchored to the entry/exit window (else 0.5×
  slope height); + rejection diagnostics (`n_rejected_*` on SearchResult,
  warn on majority-rejection); + robust axis fallback for the convergence gate.
- OMS-noncircular now consumes reinforcement dir_z (pile normal forces)
  consistently with moment/GLE paths; legacy-fallback-with-reinforcement warns.
- drilled_shaft adapter METHOD_INFO gaps (rational selectors on
  capacity_vs_depth, pa on lrfd) documented.
**Refuted:** the >80° jaggedness-gate concern (real active-wedge scarps are
≤45°+φ/2 ≈ 65°; >80° = tension crack, never a sliced shear base).
**Deferred cleanups (no correctness impact):** newmark ky bisection rebuilds
slices per trial; pdf_import join/snap O(n²) (spatial hash); rapid-drawdown
O(n²) slice alignment; pdf adapter build_* copy-paste helper; fem2d adapter
duplicate scheme validation; _lininterp reuse within soe; PA constant dup;
test unit-constant dedup via geotech_common.units.

## Log
- 2026-07-05: plan written; A1 dispatched (staggered agent). Slide2 manual
  downloaded + 21 problems extracted to module_work/slope_v53/.
- 2026-07-05: A2 DONE — MSE LRFD external-stability CDR path (V-009). Additive
  `check_external_stability_lrfd` + `analyze_mse_wall(lrfd_external=True)` +
  `mse_lrfd_external_stability` adapter method; reproduces the GEC-11 E4 CDRs
  through the high-level path (no hand-fed factors); default ASD path unchanged.
