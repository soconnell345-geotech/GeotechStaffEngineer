# v5.2 coverage backlog — build + validate (offline, no Funhouse)

**Goal:** close the capability gaps that Phase E surfaced (documented as
`N/A (scope)` / `CONVENTION` in `validation_examples/RESULTS.md`). Each item is
pure-Python and has an **exact published validation target already in
`validation_examples/INVENTORY.md`**, so every build is verified offline and
flips its `RESULTS.md` row to a green `PASS`. No API / Funhouse needed.

**Branch:** `v5.2-coverage` (from master `cb69142`). One capability per commit;
each commit adds/updates the module + a validation test pinning the published
target. Run `pytest validation_examples/ -q` + the touched module's suite after
each. Merge to master at batch milestones. **NO version bump / tag / publish**
(owner-gated). Venv: `C:\Users\socon\OneDrive\dev\GeotechStaffEngineer\.venv\Scripts\python.exe`.

## Working rules
- **Do not break existing behavior.** New methods/params are additive and
  default-preserving (mirror the fem2d Phase-E additions). Existing module
  suites must stay green.
- **Validate against the published target, don't tune to it.** Implement the
  documented method; if the published value needs a convention choice, expose
  it as a param with a sane default.
- Stagger agents one at a time (usage window). Orchestrator reviews each diff,
  gates on tests, commits.

## Batch 1 — quick wins (low risk, exact targets)
- [ ] **Q1 Hough granular settlement** (`settlement`). Add the C′-index method
      `dH = H/C' · log10((σ'vo+Δσ)/σ'vo)` with 2:1 stress (primitive
      `approximate_2to1` exists). Target: GEC-6 Table B1-3, 12 q×B cells
      (e.g. B=3 m: 21/25/28/30 mm; worked B=3,q=240 → 21 mm). Flips V-022.
- [ ] **Q2 Meyerhof (1976) pile-group settlement** (`pile_group`). Closed form
      `S(in) = 4·pf(ksf)·If·√B(ft)/N160`, `If = 1 − (2/3·DB)/(8B) ≥ 0.5`.
      Target: V-005 → 1.04 in (also 0.95 in @ 55 ft). Flips V-005.
- [ ] **Q3 axial_pile per-layer toe-φ + head-depth offset** (`axial_pile`).
      Optional per-layer `toe_friction_angle` (GEC-12 design-limit toe φ) and a
      head-depth/embedment offset so a footing-datum profile isn't hand-clipped.
      Target: V-001 toe plateau 428.1 kips (single-φ API currently −30%).
- [ ] **Q4 MSE bar-mat Kr/F\* curves** (`retaining_walls`). Add a bar-mat/grid
      branch to `Kr_Ka_ratio` (2.5→1.2 over 0–20 ft) and `F_star_metallic`
      (20·t/St → 10·t/St). Target: Table E4-7.4 (Tmax 6.25→19.05 k/panel,
      F*=0.955 @ L4). Flips the V-010 CONVENTION rows.

## Batch 2 — real builds (after Batch 1; consider compacting first)
- [ ] drilled_shaft rational GEC-10 chains (OCR β=0.41, su-transform α=0.47) — V-006/007
- [ ] MSE LRFD external-stability path (CDRs) — V-009
- [ ] soe basal-heave w/ sidewall shear (FS=1.54) + FHWA apparent-pressure anchored wall + log-spiral passive — V-013/014
- [ ] lateral_pile full Reese (1974) sand p-y (3.3 mm) — V-017
- [ ] fem2d monolithic u-p consolidation (undrained transient) — V-023 [hardest]

## Deliberately deferred / owner-call
- drilled_shaft large-D base reduction default (V-008b), bearing `compute()`
  depth-factor + GW defaults (V-021): module is defensibly correct; only change
  as opt-in flags if the owner wants reference-matching defaults.
- wave_equation diesel combustion model (V-003): large build, skip unless owner
  needs diesel drivability tie-outs.
- RAP upper-zone stiffness settlement (V-018), Priebe convention (V-019): medium;
  fold into a later ground_improvement pass.

## Log
- 2026-06-15: branch created from cb69142; plan written. Batch 1 starting
  (Opus, orchestrating staggered agents). Resume: read this file +
  `validation_examples/RESULTS.md` + `INVENTORY.md`; continue the first
  unchecked Batch-1 item.
