# v5.0 — `deepagents` Migration Plan

> **Status:** ACTIVE plan for the `v5.0-deepagents` branch.
> **Decision (2026-06-05):** owner chose the **strangler-behind-a-flag** approach and
> greenlit dispatching the offline Phase 0–1 build now.
> **Current release:** 4.6.5. **Target:** 5.0.0.
> **Scope:** the *agent layer* only (`funhouse_agent/`). The 36 analysis modules, the
> `geotech-references` submodule, and the validated math are **not** rewritten — they
> are the asset we wrap, not replace.

---

## 1. Executive summary (plain language)

Today the "brains" that lets an LLM drive our 900+ geotechnical calculations is a
**hand-built agent loop** (`funhouse_agent/`). It works and has genuinely smart pieces,
but a meaningful chunk is plumbing we maintain by hand: reading the model's replies,
guessing whether a reply is "a step" or "the final answer," managing conversation
memory, and wiring up our reviewer and reference-lookup helpers one by one.

`deepagents` is a now-mature open-source **agent harness** (LangChain team, currently
**v0.6.8**) that provides that plumbing as a maintained, batteries-included library.
The pieces we hand-rolled — the loop, memory management, sub-agents — are its built-in
features, plus capabilities we don't have today (**planning, a scratch/memory
filesystem, automatic context summarization, durable resumable sessions, live
streaming, and human-approval gates**).

**This is NOT a from-scratch rewrite.** We keep everything valuable — our 52
calculation adapters and the "discovery" tool design — and swap out *only* the brittle
hand-built loop for the `deepagents` engine, building it **side-by-side behind a flag**
so 4.6.5 keeps shipping untouched and we prove the new agent is better on our own
68-question test set before cutover.

**Headline gains:**

| Win | Why it matters |
|-----|----------------|
| Delete the brittle text-parsing loop | Removes the root cause of the known **"fabricated success on tool error"** bug (HANDOFF P1) |
| **Planning** (`write_todos`) | Agent lays out + tracks multi-step jobs (classify → SPT → bearing → settlement); more reliable, more transparent |
| **Cross-session memory** (`StoreBackend`) | Remember a project's soil profile / prior results between sessions |
| **Context summarization + file offload** | Long "run 12 analyses and compare" sessions stop blowing the context window |
| **Cleaner specialist sub-agents** | `consult_references` + reviewer become first-class isolated sub-agents; new ones (report-builder, QC) become trivial |
| **Durable + streamable + human-in-the-loop** | Resume a session, watch the agent reason live, optionally approve a calc before it runs |
| **Less code we own** | Inherit LangChain/LangGraph's maintained loop + LangSmith tracing |

**Costs (honest):** a heavy dependency (`langchain`/`langgraph`) **in the agent layer
only** (core scientific package stays clean — ship it as an extra); re-bridging the
Databricks/Funhouse `PrompterAPI` backend through LangChain (the main risk — §8); a
rewrite of the notebook chat UI; re-pointing eval/triage tooling at the new agent.

---

## 2. Where we are today

The agent lives in `funhouse_agent/`, self-contained (no `foundry/` dependency).

| File | Role | Disposition |
|------|------|-------------|
| `agent.py` (733) | `GeotechAgent` w/ **two** hand-rolled loops: `_ask_react` (`<tool_call>` XML parse + `_looks_like_reasoning` heuristics + nudge) and `_ask_native` (manual OpenAI `tools` loop) | **Replace** w/ `create_deep_agent` |
| `react_support.py` (165) | XML parser, `ConversationHistory` string-packer, `_truncate` | **Delete** (LangGraph owns state) |
| `engine.py` (379) | `GenAIEngine` Protocol + `ClaudeEngine`/`NativeToolEngine`/`PrompterBridgeEngine` + hybrid SDK shim | **Re-bridge** to a LangChain chat model (§8) |
| `dispatch.py` (354) | 4 meta-tools over `MODULE_REGISTRY` + alias/fuzzy resolution | **KEEP** — wrap as LangChain tools |
| `native_tools.py` (343) | OpenAI tool schemas | **Mostly delete** (LangChain generates schemas) |
| `reviewer.py` (261) | `consult_references` sub-agent + post-hoc `run_review` | **Re-express** as `SubAgentMiddleware` subagents |
| `system_prompt.py` (208) | Domain guidance + ReAct boilerplate + module catalog | **Trim** (drop ReAct/`<tool_call>` sections) |
| `vision_tools.py` (307) | `analyze_image`/`analyze_pdf_page`/`read_reference_figure`/`save_file` | **KEEP** as custom tools |
| `notebook.py` (430) | `NotebookChat` ipywidgets UI | **Rewrite** on LangGraph streaming |
| `adapters/` (52) | flat-JSON → module bridges (`METHOD_INFO` + `METHOD_REGISTRY`) | **KEEP UNCHANGED** — the asset |

### Two design ideas worth preserving
1. **Discovery-based tool model** — 4 meta-tools (`list_agents`→`list_methods`→
   `describe_method`→`call_agent`) over a 900-method registry, instead of 900 tools. A
   good answer to "too many tools." **Keep it.**
2. **Reference access via a scoped sub-agent** — `consult_references` isolates the 21
   reference modules off the primary's surface. Exactly the **context-isolation**
   pattern `SubAgentMiddleware` is built for — a 1:1 port.

---

## 3. `deepagents` today (v0.6.8, June 2026)

A composable **middleware stack on LangGraph**. `create_deep_agent(model, tools,
system_prompt, subagents, ...)` auto-attaches:

- **`TodoListMiddleware`** — `write_todos` planning tool + prompting.
- **`FilesystemMiddleware`** — `ls`/`read_file`/`write_file`/`edit_file` over
  `StateBackend` (ephemeral), `StoreBackend` (persistent across threads),
  `CompositeBackend` (route `/memories/` → persistent). `read_file` returns
  images/PDFs/audio as multimodal content blocks.
- **`SubAgentMiddleware`** — a `task` tool + custom subagents, each a dict
  `{name, description, system_prompt, tools, model?, middleware?}`. Point = **context
  isolation**. (Advanced: `CompiledSubAgent` for a custom graph; async/remote subagents
  via the Agent Protocol since v0.5.)
- **`SummarizationMiddleware`** — auto-compresses history near token limits.
- Opt-in: `ToolRetryMiddleware`, `Model/ToolCallLimitMiddleware` (runaway guards),
  `HumanInTheLoopMiddleware`, model fallback, PII redaction, Anthropic/OpenAI
  prompt-caching middleware.

From LangGraph: **streaming, checkpointing/persistence (threads), durable resume,
LangSmith tracing**. Model-agnostic (any tool-calling LLM via a LangChain chat model).

---

## 4. Component mapping

| Today (custom) | v5.0 (`deepagents`) | Effect |
|---|---|---|
| `_ask_react` XML loop + heuristics + nudge | LangGraph tool loop | **−~300 ln; kills P1 fake-success** |
| `_ask_native` manual loop | same built-in loop | de-duplicated |
| `ConversationHistory` + `_truncate` (8k) | message state + `SummarizationMiddleware` | no silent truncation |
| `GenAIEngine` + 3 engine classes + SDK shim | one LangChain chat-model wrapper (§8) | fewer parts; model-agnostic |
| 4 meta-tools (`dispatch.py`) | 4 LangChain `@tool`s over the **same** fns | preserved |
| `consult_references` (manual) | `references` **subagent** | cleaner isolation |
| `reviewer.run_review` | `reviewer` **subagent** / HITL | cleaner; optional human gate |
| `vision_tools.*` | custom tools + multimodal `read_file` | kept |
| *(none)* | **`write_todos` planning** | **new** |
| *(none)* | **`StoreBackend` memory** | **new** |
| *(none)* | **checkpointer / durable threads / streaming** | **new** |
| `MODULE_REGISTRY` + 52 adapters | **unchanged** | asset preserved |

---

## 5. Recommended approach — strangler, behind a flag *(chosen)*

Build `funhouse_agent` v2 **alongside** v1. New code under `funhouse_agent/deep/`
wraps the **same adapters** as LangChain tools and stands up `create_deep_agent`,
selected behind a flag (`GeotechAgent(backend="deep")` / a `build_agent(...)` factory):
- 4.6.5 / `master` keeps shipping with **zero** behavior change;
- A/B the two agents on the **68-question eval** before cutover;
- v1 stays a fallback for one full release after cutover.

Maps onto the working model: **`v5.0-deepagents` = migration; `master` = 4.6.5 fixes.**

---

## 6. Phased roadmap

### Phase 0 — De-risk spike *(GATE)*
- [x] Confirm `deepagents`/`langchain`/`langgraph` install (Win dev). **Installed &
      importable:** deepagents 0.6.8, langchain 1.3.4, langgraph 1.2.4,
      langchain-anthropic 1.4.4, langchain-core 1.4.1 (in the shared `.venv`).
- [ ] Confirm install on the Databricks runtime *(owner-in-the-loop)*.
- [ ] **Bridge Funhouse `PrompterAPI` → a LangChain chat model**; run one real
      tool-calling turn on Databricks (§8). *Decisive risk; owner-in-the-loop.*
- [x] Wrap the 4 dispatch fns as LangChain tools; minimal `create_deep_agent`; run
      **offline** against a fake chat model (no API key). **Done — 14 offline tests pass.**

### Phase 1 — Tool layer (no behavior change)
- [x] LangChain tool wrappers for `list_agents`/`list_methods`/`describe_method`/
      `call_agent` (delegating to unchanged `dispatch.py`), scope-aware via
      `make_core_tools(allowed_agents=...)`. → `funhouse_agent/deep/tools.py`
- [x] Port `vision_tools` as custom tools (`make_vision_tools`); engine/attachments/
      save_fn injected at build time. → `funhouse_agent/deep/tools.py`
- [x] Offline tests (mock model). → `funhouse_agent/deep/tests/test_deep_tools_offline.py`

### Phase 2 — Agent assembly  *(mostly done — landed early in P0–1 + the engine bridge)*
- [x] `build_deep_agent(model, ...)` via `create_deep_agent`. → `funhouse_agent/deep/agent.py`
- [x] `consult_references` → `references` subagent (scoped to `REFERENCE_MODULES`,
      holds `read_reference_figure`).
- [x] `reviewer` → `reviewer` subagent.
- [x] Port system prompt: keep domain guidance + catalog, drop ReAct/`<tool_call>`. → `prompt.py`
- [x] **Engine/vision adapter (was §10a.3):** `LangChainVisionEngine` lets ONE LangChain
      model serve both the loop and the vision tools; `build_deep_agent` auto-wraps it
      when no explicit engine is passed. → `funhouse_agent/deep/vision_engine.py`
- [x] **Databricks bridge written** (the §8 spike): `PrompterChatModel(BaseChatModel)`
      drives the Funhouse PrompterAPI OpenAI client with native tool calling +
      v1 proxy hygiene. → `funhouse_agent/deep/databricks_bridge.py`. *Live run pending.*
- [x] **Live harness:** `run_live.py` runs one real tool-calling turn end-to-end
      (`--backend anthropic` locally, or `run(PrompterChatModel(prompter=...))` on Databricks).

### Phase 3 — New capabilities  *(built + memory proven live)*
- [x] Planning — `write_todos` is ON by default (deepagents `TodoListMiddleware`);
      prompt now nudges multi-analysis planning. → `prompt.py` "Working Style".
- [x] `FilesystemMiddleware` + `CompositeBackend`: scratch in `StateBackend`, project
      memory in `StoreBackend` (`/memories/`, fixed `("memories",)` namespace).
      `build_deep_agent(store=, enable_memory=)` wires it. → `agent.py` `build_memory_backend`.
- [x] Summarization — `GeotechSummarizationMiddleware` (subclassed to avoid a name
      collision with the one deepagents auto-attaches); model-aware trigger. → `agent.py`.
- [x] LangGraph `checkpointer=` → durable resumable threads (forwarded to `create_deep_agent`).
- [x] **LIVE cross-session memory PROOF: PASS** (after a prompt fix — see Progress log).
      `run_memory_demo` (gated; in `agent.py`) for end-to-end re-checks.

### Phase 4 — UI  *(built + streaming proven live)*
- [x] `DeepNotebookChat` (`funhouse_agent/deep/notebook.py`) — ipywidgets chat driving the
      compiled agent via `stream_mode=["updates","messages"]`; pure formatters
      (`_format_update`/`_format_tool_call`/`_render_todos`), `ask_and_print` non-widget
      fallback, `from_model`, `reset`, lazy ipywidgets import. 18 offline tests; 62/62 total.
- [x] **LIVE streaming PROOF: PASS** — `ask_and_print` over real `ChatAnthropic` streamed a
      bearing-capacity run end-to-end (Vesic+Meyerhof, 1698-char answer, history kept, no
      crash). Live-only items remain (sub-agent node tokens, tool-arg-chunk shapes) — verify
      when the Databricks path is exercised.

### Phase 5 — Eval & cutover  *(harness BUILT; live run owner-gated)*
- [x] **A/B eval harness built** — `funhouse_agent/deep/eval_harness.py`: runs each suite
      question through v1 (`GeotechAgent`+`ClaudeEngine`) and v2 (`build_deep_agent`),
      scores the **P1 hallucination-on-error rate** (headline) + tool-error rate, rounds,
      latency, tokens; renders an A/B markdown table + results JSON. CLI: `--dry-run`
      (offline), `--limit N` (cheap subset), `--yes` (non-interactive), `--model`. 20 offline
      tests; **82/82** total. Offline dry-run verified end-to-end.
- [ ] **Run the live A/B eval** *(owner-budget-gated)*. NOTE: the suite is now **76**
      questions (grew from 68), so a full run = **76×2 = 152** real model runs, ~**$20**
      (range $10–40, Sonnet). Cheap subset: `-m funhouse_agent.deep.eval_harness --limit 5
      --yes`. Correctness is **NOT auto-scorable** (suite carries no expected answers) — the
      P1 rate + process metrics are the comparison; add an LLM-judge for correctness if wanted.
- [ ] Flip default to v2 when v2 ≥ v1 on every axis; keep v1 one release.
- [ ] Re-point `triage_feedback.py` / `module_feedback.json` at v2.
- [ ] Tag **5.0.0** — only on the owner's explicit say-so (no bump/PyPI before then).

---

## 7. Mapping `consult_references`/`reviewer` → subagents (concrete)

```python
# sketch — funhouse_agent/deep/agent.py
from deepagents import create_deep_agent
from funhouse_agent.deep.tools import CORE_TOOLS, VISION_TOOLS, REFERENCE_TOOLS

agent = create_deep_agent(
    model=model,                       # LangChain chat model (§8)
    tools=CORE_TOOLS + VISION_TOOLS,   # 4 meta-tools + vision, over the SAME adapters
    system_prompt=DOMAIN_PROMPT,       # trimmed system_prompt.py (no ReAct boilerplate)
    subagents=[
        {
            "name": "references",
            "description": "Reference librarian: NAVFAC DM7, FHWA GEC, UFC, micropile, "
                           "FEMA, NOAA. Cites section/table/figure; reads chart values "
                           "off rendered figures.",
            "system_prompt": CONSULTANT_FRAMING,         # from reviewer.py
            "tools": REFERENCE_TOOLS,                    # meta-tools scoped to REFERENCE_MODULES + figure read-off
        },
        {
            "name": "reviewer",
            "description": "Senior QC reviewer: checks a finished analysis against the references.",
            "system_prompt": REVIEWER_SYSTEM_PROMPT,     # from reviewer.py
            "tools": REFERENCE_TOOLS,
        },
    ],
)
```
The primary's `consult_references` tool call becomes a `task(subagent="references", ...)`
delegation; scoping that today lives in `allowed_agents` is reused verbatim by giving
the subagent only the reference-scoped tools.

---

## 8. The one technical risk — the Funhouse `PrompterAPI` bridge

Everything downstream depends on this. Today `NativeToolEngine` wraps `prompter.client`
(a raw OpenAI client behind the Databricks/Funhouse proxy) and sends **explicit-dict**
messages because the proxy "can carry null fields (function_call, refusal, audio,
annotations) that some servers reject." LangChain must drive that same proxy. Options,
easiest first:
1. **`ChatOpenAI(base_url=..., api_key=..., default_headers=...)`** at the Funhouse
   endpoint — if the proxy is OpenAI-compatible enough, this "just works."
2. **A thin custom `BaseChatModel`** calling `prompter.client` directly (mirrors today's
   `NativeToolEngine`, preserving message hygiene). Most control; ~1 small class.
3. If the official `funhouse` SDK ships a LangChain adapter (it already ships engine
   classes — `USING_SDK_ENGINES`), prefer that.

**Phase 0 must prove one real multi-tool turn end-to-end on Databricks** — needs the
owner's env + API budget (a subagent on this box can't reach Databricks). The **local**
real-model turn is already proven (see Progress log: `ChatAnthropic` PASS). Only the
proxy-specific turn remains.

**Constraint (from the SDK):** `PrompterChatModel` requires `prompter.client` to be
non-`None`. The SDK's `NativeToolEngine` raises otherwise; backends without a client
(e.g. bare Grok JSON) would need the `PrompterBridgeEngine`/`prompter.chat(tools=...)`
path instead, which does NOT fit the LangChain `BaseChatModel` tool loop. Confirm the
target Databricks backend exposes `.client`.

---

## 9. Parallel working model (this branch vs. 4.6.5)

- **`master`** = the 4.6.5 line: small fixes, the open `module_feedback.json` per-module
  bugs, the reference-layer QC backlog — all continue unchanged.
- **`v5.0-deepagents`** = this migration. Subagents do Phase 0–1 **offline** here; the
  live Databricks validation is owner-in-the-loop.
- No `funhouse_agent` v1 file is modified until Phase 5 cutover, so the lines don't
  collide. `adapters/` is shared and **read-only** to the migration.

---

## 10a. Phase 0–1 findings (2026-06-05) — build notes for Phase 2

The offline tool layer + agent assembly are built and green (`funhouse_agent/deep/`).
Concrete things Phase 2 must carry forward:

1. **`call_agent` needs an explicit `extra="allow"` args schema.** A bare `**kwargs`
   signature does NOT survive `StructuredTool.from_function` (LangChain collapses it to
   one `extra` field and drops top-level keys). We declare `_CallAgentArgs(BaseModel)`
   with `ConfigDict(extra="allow")` so the model's flattened method params flow through
   and get auto-nested — same robustness as v1's `dispatch_native_tool`. Keep this.
2. **The deepagents `task` tool + a built-in `general-purpose` subagent are ALWAYS
   present** and can't be removed. So `reference_mode="off"` can only omit our *named*
   `references`/`reviewer` subagents — it can't remove `task`. The v1 `"after_calc"`
   gating (offer reference access only after a calc runs) has **no direct equivalent**;
   it would need custom middleware. Decide in Phase 2 whether that gating still matters.
3. **`model=` (LangChain chat model) and `engine=` (Funhouse vision) are two different
   objects.** Vision tools call `engine.analyze_image(bytes, prompt)` — a `GenAIEngine`
   method, not a `BaseChatModel` method. Phase 2 must either pass a Funhouse engine
   alongside the LangChain model, or write a small adapter exposing `analyze_image` over
   the LangChain multimodal message API. (`read_reference_figure`/`save_file` work
   without a live engine; `analyze_*` return a clean error when `engine=None`.)
4. **Attachments are build-time-closured, not per-`ask()`.** v1 mutates
   `self._attachments` each turn; here they're captured at `build_deep_agent` time. If
   per-invocation attachments are needed, wire them through deepagents state/context
   (or rebuild tools per request).
5. **Vision-tool arg fidelity** (`read_reference_figure(reference, figure_number,
   prompt)`) should be re-checked against `vision_tools` exact arg contract in Phase 2
   (offline tests only assert it errors cleanly without an engine).
6. **Full agent-loop test needs a real tool-calling model.** `GenericFakeChatModel`
   builds the graph but doesn't emit structured `tool_calls`, so it can't drive
   model→tools→model. Use `ChatAnthropic` (API key) or the Databricks-proxied model.

## 11. Progress log

- **2026-06-08 — `v5.0.0-rc3`: GPT 5.4 (`funhouse-gpt-high`) support.** Owner is testing in
  Funhouse on the new GPT 5.4. No structural change needed — `PrompterChatModel` already
  takes `model="funhouse-gpt-high"` (verified the override flows through `bind_tools` →
  request). Hardened the bridge for GPT-5/reasoning quirks: `temperature` is now omittable
  (field made `Optional`, `temperature=None` drops it), and `_create_with_param_fallback`
  retries ONCE if the model rejects `temperature` (dropped) or wants `max_completion_tokens`
  instead of `max_tokens` (renamed) — the first attempt is unchanged (safe for Claude), the
  retry only fires on a parameter error. Pointed all Funhouse examples at `funhouse-gpt-high`.
  7 new offline tests (93/93). Bumped → `5.0.0rc3`, re-tagged, rebuilt wheel (now the one to
  install). NOTE: rc2 also works with GPT 5.4 via the model param; rc3 just adds the auto-retry
  safety net. Could not live-test GPT here (no OpenAI/Funhouse creds) — the owner's Funhouse
  run is the live check.

- **2026-06-08 — `v5.0.0-rc2`: shipped a notebook self-check + bundled the eval suite.**
  Owner asked whether the wheel comes with tests. The pytest suite is dev-only (not
  shipped), so added `funhouse_agent/deep/selfcheck.py` — `run_selfcheck(model)` runs the
  live proofs (agent tool-call + cross-session memory) and prints a PASS/FAIL summary; it's
  the notebook-friendly "is it working?" check. Also bundled `geotech_test_suite.json` as
  package-data so the A/B `eval_harness` works from an installed wheel. Bumped → `5.0.0rc2`,
  re-tagged, rebuilt the wheel (suite JSON + selfcheck IN, tests OUT). 86/86 offline tests.
  **Live self-check PASS (2/2)** via `python -m funhouse_agent.deep.selfcheck`. The rc2 wheel
  is the one to install. (rc1 superseded.)

- **2026-06-08 — Live A/B SUBSET eval run + TEST pre-release `v5.0.0-rc1`.**
  Per lead feedback: ran the **cheap subset only** (`--limit 5`, ~$2; lead runs full reviews
  in Funhouse). Results (5 Qs: 4 bearing_capacity + 1 settlement, claude-sonnet-4-6):
  | metric | v1 | v2 |
  |---|---|---|
  | **P1 hallucination-on-error** | **0.0%** | **0.0%** |
  | tool-error rate | 40% | 60% |
  | errors/q | 0.40 | 1.20 |
  | avg rounds | 2.2 | 4.0 |
  | avg latency | 95.8s | **37.1s** |
  | tokens/q | n/a (v1 ClaudeEngine) | ~62.9k |
  Read: **no hallucination regression** (0% both — the headline safety metric), v2 **~2.6×
  faster**, but v2 logs **more tool errors / rounds** on this tiny sample (likely the
  recoverable "guess method → describe_method errors → correct call" pattern, amplified by
  the references/reviewer sub-agents). FOLLOW-UP: confirm those v2 errors are recoverable
  (P1=0% suggests yes) vs real wrapper dispatch bugs — needs the full Funhouse review.
  - **TEST pre-release prepared:** bumped `pyproject` → `5.0.0rc1`, added a `deep` extra
    (deepagents/langchain/langgraph/langchain-anthropic) — OPT-IN; `import funhouse_agent`
    still returns the stable v1 agent. Tagged **`v5.0.0-rc1`** on `v5.0-deepagents` + built a
    wheel so the owner can exercise v2 in Funhouse without disturbing the stable line. NO
    final 5.0.0 / NO PyPI publish without explicit owner OK.

- **2026-06-08 — Phase 5 A/B eval harness built (offline-verified).**
  `funhouse_agent/deep/eval_harness.py` runs the suite through v1 + v2 and scores the **P1
  hallucination-on-error rate** (headline metric: error in trace + confidently-successful
  answer), plus tool-error rate / rounds / latency / tokens; emits an A/B table + JSON. CLI
  `--dry-run`/`--limit`/`--yes`/`--model`. 20 new offline tests; **82/82** total; offline
  dry-run produces the table end-to-end. Findings: (1) the suite is **76 questions** now,
  not 68 → full run = 152 model calls, ~$20. (2) Suite is **question-only** → numeric
  correctness NOT auto-scorable; P1 + process metrics are the comparison (LLM-judge hook
  left). (3) P1 rate is an **upper bound** — v1's "guess method → describe_method errors →
  correct call" pattern inflates it; read it next to the recovered-vs-fatal split. (4) v1
  `ClaudeEngine` doesn't expose token usage (v2 does via `usage_metadata`); a live run must
  confirm sub-agent (references/reviewer) tokens+errors surface in the top-level result.
  **Live A/B run is owner-budget-gated (not yet run).**

- **2026-06-08 — Phase 4 (notebook UI) built + streaming PROVEN LIVE.**
  `DeepNotebookChat` (`funhouse_agent/deep/notebook.py`) drives the compiled agent via
  `stream_mode=["updates","messages"]` — pure formatters, `ask_and_print` non-widget
  fallback, `from_model`, `reset`, lazy ipywidgets import (ipywidgets is NOT installed in
  the venv, which makes the lazy-import guarantee testable). 18 new offline tests; **62/62**.
  - **Stream contract (for Phase 5 / live):** `("messages", (AIMessageChunk, metadata))` —
    surface a token as answer text only when `metadata["langgraph_node"]=="model"` and the
    chunk has no `tool_call_chunks`. `("updates", {node: {state_key: value}})` — node `model`
    emits `AIMessage.tool_calls` (`call_agent`{agent_name,method,parameters} / `task`
    {description,subagent_type} / `write_todos`); node `tools` emits `{"messages":[ToolMessage],
    "todos":[{content,status}]}`.
  - **LIVE streaming proof PASS:** `ask_and_print` over real `ChatAnthropic` streamed a
    bearing-capacity run (Vesic+Meyerhof, 1698-char answer, 2-msg history, EXIT 0).
  - Fixed a Windows cp1252 crash (Unicode `→`→ASCII `->` + encoding-safe emit).
  - Live-only unknowns still open: sub-agent (`task`-delegated) token `langgraph_node` value,
    Anthropic streamed block-list vs string content, `tool_call_chunks` accumulation.

- **2026-06-06 — Phase 3 built + cross-session memory PROVEN LIVE.** Extended
  `build_deep_agent` with `store`/`checkpointer`/`enable_memory`/`enable_summarization`
  (defaults preserve prior behavior). Planning + scratch filesystem are ON by default
  (deepagents); `/memories/` routes to a `StoreBackend` (fixed `("memories",)` namespace)
  via `CompositeBackend`; a subclassed `GeotechSummarizationMiddleware` avoids a
  name-collision with the auto-attached one. **44/44 offline tests pass** (backend memory
  round-trip is a real store persist, not config-only).
  - **Live memory demo — first run FAILED, then fixed.** Root cause was NOT a wiring bug:
    the store persisted fine, but the agent saved to `/memories/project_context.md` while
    only `/memories/AGENTS.md` is auto-loaded into the `<agent_memory>` block — so the next
    session saw empty memory. **Fix = prompt (`prompt.py`):** canonicalize durable memory on
    `/memories/AGENTS.md` (the auto-loaded file) + tell the agent to `ls /memories/` on a
    fresh session. **Re-run PASS:** thread 2 (new `thread_id`, shared store) recalled "GWT =
    3.5 m below grade" from auto-loaded memory. Lesson: the auto-load target and the agent's
    save target must be the same file.

### Earlier

- **2026-06-05** — Plan written; owner chose strangler-behind-a-flag + dispatch
  offline Phase 0–1. Env verified (deepagents 0.6.8 stack). Built
  `funhouse_agent/deep/` (`tools.py`, `prompt.py`, `agent.py`, `__init__.py`, offline
  tests). **14/14 offline tests pass.** No v1 file touched. Findings in §10a.
- **2026-06-06** — Phase 2: resolved §10a.3 with `LangChainVisionEngine` (one model
  serves loop + vision; `build_deep_agent` auto-wraps). Wrote the §8 Databricks bridge
  `PrompterChatModel` (mirrors v1 proxy hygiene) + `run_live.py` one-turn harness.
  **30/30 offline tests pass** (16 new). Still no v1 file touched.
- **2026-06-06 — LOCAL LIVE PROOF: PASS.** Ran `run_live.py --backend anthropic`
  (`ChatAnthropic`, claude-sonnet-4-6) end-to-end. The deepagents loop autonomously
  discovered the tool, fired `call_agent → bearing_capacity`, ran **Vesic + Meyerhof**,
  and returned a strong answer (method comparison, term breakdown, depth factors, FOS,
  confidence + groundwater caveat). Asserts: `call_agent` invoked ✓, numeric answer ✓.
  → The core architecture (discovery tool model + dispatch + deepagents loop) is proven
  with a real tool-calling model. (Aside: the agent correctly surfaced that the
  `bearing_capacity` module has no Hansen option for strip shape — a real module note.)
- **SDK cross-check:** read `Funhouse_for_Reference/funhouse-sdk-python/funhouse/services/
  prompter/engine.py`. Confirms `NativeToolEngine` drives `prompter.client.chat.
  completions.create(...)` with `model=prompter.chat_model` — exactly the surface
  `PrompterChatModel` targets. **The SDK ships NO LangChain adapter**, so our bridge is
  necessary. Constraint to honor: `NativeToolEngine` raises if `prompter.client is None`
  (some backends have no client) — our bridge assumes a client too.
  **Only remaining live step:** run the Databricks proxy proof in a notebook —
  `from funhouse_agent.deep.databricks_bridge import PrompterChatModel; from
  funhouse_agent.deep.run_live import run; run(PrompterChatModel(prompter=fh_prompter))`
  — to settle the 5 proxy-only uncertainties in §8.

---

*Living plan — check items off as phases land.*
