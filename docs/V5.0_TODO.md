# v5.0 deepagents — Open TODO / Handoff Backlog

> **Status:** the `v5.0-deepagents` branch is **wrapping up** (latest `v5.0.0-rc8`, commit
> `4ef7ba9`, 142 offline tests + 1 skip green, Funhouse-validated on GPT 5.4). Work is
> **additive** — all new code lives under `funhouse_agent/deep/`; v1 is untouched. This is
> the backlog to carry forward into the merge with the other branches' work.
> See `docs/V5.0_DEEPAGENTS_MIGRATION.md` for the full plan + progress log.

Source of most items: the live **76-question Funhouse run on GPT 5.4** (rc7). Headlines:
76/76 completed, **0 exceptions**, 6.64M total tokens (median 46k / mean 87k), P1 7.9% (6/76,
likely mostly false positives).

---

## A. Token efficiency

- [x] **(HIGH) Cap the references sub-agent's internal rounds.** *(DONE, v5.1 — `funhouse_agent/deep/limits.py` `ModelCallBudgetMiddleware` on `build_references_subagent`, default 8 calls/consult, forced summarize-and-answer final turn + graceful backstop; offline-tested.)* The single biggest lever:
      **26/76 reference-consult questions burn 69% of all tokens** (4.6M / 6.6M), almost
      entirely *input* tokens, because the references sub-agent re-sends its accumulated
      context across many internal model calls. rc7 capped each *result's* size, not the
      *number of rounds*. Add a `ModelCallLimitMiddleware` (or a round budget) to
      `build_references_subagent` in `funhouse_agent/deep/agent.py`. Expect ~halving of
      reference-question cost with little quality loss.
- [x] **(MED) Smart/per-tool truncation + a "if truncated, search narrower" prompt nudge.** *(DONE, v5.1 — `reference_result_chars` (default 16k) for reference reads, calc `call_agent` results uncapped, truncation marker carries a "search narrower" nudge; offline-tested.)*
      The reference-completeness option the owner deferred ("OK for now"): keep a larger
      truncation budget for *reference* reads, keep numeric/calc results uncapped (already
      tiny), and tell the agent to do a narrower follow-up if a result was truncated.

## B. Module API ergonomics  *(cost + P1 inflation; the existing `module_work/module_feedback.json` backlog)*

- [ ] **(HIGH) Parameter/method-name ergonomics for the error hotspots.** The agent fumbles
      the *API* (not the engineering) on: **geolysis** (GL-2: 12 tool errors, GL-1: 4),
      **ground_improvement** (GI-1: 8, GI-2: 7), **soe** (SOE-1: 7); also wind_loads, gstools,
      settlement, lateral_pile, pile_group (4–5 errs each). It recovers every time (0 crashes)
      but it costs rounds/tokens and inflates P1. rc8 fixed the *method-name* guessing; the
      *parameter-name* half remains — feed these into `module_work/module_feedback.json`.
- [x] **(DONE, rc8)** `describe_method` resolves guessed method names (vesic/meyerhof/… →
      real method docs + redirect note). Removes a wasted round/question.

## C. Eval-suite gaps

- [ ] **(MED) File-input modules aren't exercised.** Eight questions ran at **0 rounds / ~9k
      tokens** — ags4, pygef, hvsrpy, pdf_import, dxf_import, pydiggs, swprocess, subsurface —
      because there's no actual input file to feed them. Add sample input files
      (DIGGS/GEF/AGS/DXF/PDF) to the suite (or its harness) so these modules are really tested.
- [ ] **(MED) Correctness is not auto-scored.** `geotech_test_suite.json` has no answer key,
      so the run measures *behavior*, not *accuracy*. Add expected-answer variants OR wire the
      `judge_fn` hook in `eval_harness` to an LLM-judge for a real accuracy score.
- [ ] **(LOW) Verify the 6 P1-flagged questions** (LP-2, SP-2, SS-1, SS-2, GL-1, REF-6) are
      false positives (recover-after-error) vs. anything real. Re-running on **rc8** should
      drop P1 (it clears the method-guess-driven flags) — confirm.

## D. Answer-quality review  *(owner)*

- [ ] **Spot-check answer quality** across a spread of modules from `suite_trial.json.md`:
      correctness of the numbers, and especially **citation accuracy** (reference section /
      table / equation numbers) — the reasoning is strong but pinpoint citations should be
      verified before anything lands in a spec/RFI.

## E. Phase 5 — cutover  *(all owner-gated; no version bump / PyPI without explicit OK)*

- [ ] **True v1-vs-v2 A/B in Funhouse.** `eval_harness.run_ab` / `build_agents` are wired for
      a local Anthropic key (v1 `ClaudeEngine` + v2 `ChatAnthropic`). Add a Prompter-backed v1
      engine path so a real head-to-head runs on the Funhouse model. (`run_suite` is v2-only.)
- [x] **Re-point** `module_work/triage_feedback.py` / `module_feedback.json` at the v2 agent. *(DONE, v5.1 — autodetects v2 `run_suite`/`run_ab` QAResult dicts, derives the tool sequence from the trace, keeps v1 flat-list compat; offline-tested with synthetic results JSON.)*
- [ ] **Flip the default to v2** behind the flag once v2 ≥ v1; keep v1 one release.
- [ ] **Tag final `5.0.0`** (drop the rc; decide on the PyPI publish) — explicit owner OK only.

---

## Merge / handoff notes

- **Additive & low-conflict:** all v5.0 logic is new files under `funhouse_agent/deep/`; no v1
  file (`funhouse_agent/*.py` outside `deep/`, `dispatch.py`, `system_prompt.py`, etc.) was
  modified. Merging should be clean.
- **`pyproject.toml` carries three things to preserve on merge:** `version = "5.0.0rc8"`
  (reconcile with whatever the other branches set), the **`deep` optional-dependency extra**
  (deepagents/langchain/langgraph/langchain-anthropic/`typing_extensions>=4.13`), and the
  `[tool.setuptools.package-data]` line `funhouse_agent = ["geotech_test_suite.json"]`.
- **Branch fully on `origin/v5.0-deepagents`**, latest `4ef7ba9` (rc8). 142 offline tests + 1
  skip green; no live API needed for the test suite.
- **Test wheel:** `C:\Users\socon\OneDrive\dev\v5_test_wheel\geotech_staff_engineer-5.0.0rc8-py3-none-any.whl`.
- **Databricks install gotchas (documented):** install the wheel from `/tmp` or a UC Volume
  (NOT `/Workspace`, which mangles the filename); `%pip install "...whl[deep]"` (the `[deep]`
  bracket is required); `dbutils.library.restartPython()` after install (the runtime's old
  `typing_extensions` shadows the new one until restart).
- **The §8 PrompterAPI→LangChain bridge gate is CLEARED** — proven live in Funhouse on GPT 5.4.

## rc history (all tagged on `v5.0-deepagents`)

| rc | what |
|----|------|
| rc1 | first test pre-release (opt-in `[deep]` extra; v1 stays default) |
| rc2 | notebook `run_selfcheck` + bundled the eval suite |
| rc3 | GPT 5.4 (`funhouse-gpt-high`) support + param auto-fallback |
| rc4 | pin `typing_extensions>=4.13` (Databricks Py3.12); **Funhouse self-check PASS** |
| rc5 | `run_suite` — the 4.6.4-style v2-only suite runner |
| rc6 | accurate token tracking (eval + chatbot, sub-agents included) |
| rc7 | restore tool-result truncation (token-bloat fix: BC-4 480k→80k) |
| rc8 | `describe_method` resolves guessed method names (round-saver) |
