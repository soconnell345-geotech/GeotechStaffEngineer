# Project Handoff — GeotechStaffEngineer v5.1 (2026-06-14)

> **Why this file exists.** Work on this v5.1 effort was being done with Claude
> Fable 5. Due to a government access restriction the session is being
> downgraded to Claude Opus mid-project. This document captures the complete
> state — what is finished and durable, what was underway, and exactly how to
> resume — so the next session (any model) can pick up cleanly or make an
> informed decision to **pin and wait** for Fable access to return.
>
> Companion docs: `module_work/WEEKEND_QC_2026-06-13.md` (the weekend QC board),
> `validation_examples/INVENTORY.md` (the 25 harvested validation problems),
> and the per-module `UPGRADE_PLAN.md` / `VALIDATION.md` / `DESIGN.md` files.

---

## 1. One-screen status

| Item | Value |
|------|-------|
| Repo | github.com/soconnell345-geotech/GeotechStaffEngineer (private) |
| **master HEAD** | **`a9f81a1`** — pushed to origin ✅ |
| Submodule `geotech-references` | **`99e67b7`** — pushed to origin/main ✅; superproject pointer recorded + working tree synced |
| Working branch | `worktree-v5.1-todos` (this worktree) — fully merged into master as of this handoff |
| **Version string** | `5.1.0rc3` in `pyproject.toml` — **committed string only, NO git tag** |
| Test wheel | `C:\Users\socon\OneDrive\dev\v5_test_wheel\geotech_staff_engineer-5.1.0rc3-py3-none-any.whl` (rc1/rc2 deleted) |
| Full repo suite | **7981 passed, 48 skipped** (`pytest -q`, ~37 min, baseline 2026-06-13) |
| Submodule suite | **3703 passed** (`pytest tests/` in geotech-references) |
| **Publish status** | **UNRELEASED.** 5.1.0 final tag/PyPI publish is **owner-gated.** |

**⚠️ Release gate (do not trip):** a `v*` git tag push **auto-publishes to PyPI**
via `.github/workflows/publish.yml` (OIDC trusted publishing). The rc versions
were committed as version strings with **no tag** on purpose. Do **not** push a
tag, bump to a final version, or publish without the owner's explicit OK.

---

## 2. The v5.1 arc since v5.0 — what shipped (all merged to master)

v5.0.0 was released to PyPI 2026-06-09 (deepagents migration). Everything below
is the v5.1 work on top of it (63 commits, `310ed3a..a9f81a1`). **None of it is
released** — it lives on master and in the rc3 test wheel only.

### 2a. v5.1 TODO sweep (the original ask) — DONE
Commits `67d230c, 0d52d40, cac58b6, 697a58e, b506b76, c578ee8, 39a872c, f24b03b, 0bdfa82`.
Cleared the entire v5.1 backlog via scoped subagents: references sub-agent round
cap + per-tool truncation budgets; foundations / deep-foundation / retention /
seismic QC (incl. a Mononobe-Okabe battered-wall fix, Smith springs, stickup,
pile_group convention); adapter ergonomics (param aliases, `allowed_values`
rollout, new-param exposure); eval-suite sample files + answer-key auto-scoring;
references review + submodule bump; foundry staleness audit (5 drifted wrappers).

### 2b. LE + FEM modernization ("prove improved productivity") — DONE
- **slope_stability** (`05bb91d`→`83d4633`, merged `83d4633`, knobs `2789278`):
  rigorous GLE/Morgenstern-Price (Fredlund-Krahn), Janbu+f0, entry-exit +
  differential-evolution noncircular search, reinforcement (nails/anchors/
  geosynthetics) in all methods, probabilistic FOS (FOSM Duncan-2000 + Monte
  Carlo), SHANSEP + Hoek-Brown, ponded water, per-slice force tables. Fixed
  pre-existing SS-4 (Bishop direction) + SS-5 (below-base fragment) bugs.
  Validated: F&K 1977 within 0–1.5%, ACADS 1(a) 0.985–0.989 vs 1.00.
- **fem2d** (`b328bc0`→`394d162`): T6 quadratic elements (default for collapse;
  CST locks — Prandtl Nc≥9 vs T6 5.10–5.25 vs exact 5.14), 3D principal-stress
  Mohr-Coulomb return mapping (Clausen/de Souza Neto), Griffiths-Lane-grade SRM
  (FOS 1.34–1.37 vs published 1.4), 19× faster solver, depth-aware `x_extend`
  default (old default inflated FOS up to +91%).
- **TRACKED FOLLOW-UP (not a regression, not finished):** SRM failure-detection
  mesh consistency on low-c/low-φ faces. Knobs `nr_method`/`nr_fallback` (opt-in,
  **not validated**) + `stall_window` are in place; B6 cross-check pinned to
  56×20 meanwhile. Full notes in `fem2d/UPGRADE_PLAN.md`.

### 2c. Reliability module (consolidation #7) — DONE
Commits `59fbc6d`→`f7639e5`. New top-level `reliability/` package: RandomVariable
model (4 dists, truncation, correlation), four engines (FOSM with Duncan
variance table, Rosenblueth PEM, Monte Carlo w/ LHS + Nataf-lite, native FORM =
HL-RF + Rackwitz-Fiessler matching pystra to 4 decimals), 43-entry
provenance-locked COV database (Duncan 2000 / ISSMGE TC304 / Phoon-Kulhawy),
Vanmarcke spatial averaging, bearing/axial-pile/slope wrappers, funhouse adapter
(7 methods). 176 tests. **The owner's prime long-term interest** — next
increments recommended: random-field FEM via gstools, system reliability, LRFD
calibration.

### 2d. Calc-package visualization (calc-viz) — DONE
Commits `00911f4`→`f758ddb`. slope_stability `plotting.py` + `calc_steps`
rewrite (SLOPE/W-style figures), fem2d calc-package integration (PLAXIS-style),
`calc_package/interactive.py` single-file plotly viewers (`save_interactive_report`,
`interactive` extra). Demos in `docs/examples/` (4 HTML files). **Full modeling
GUI parked** (agent estimate 2–4 wk for a browser LE editor; geo_project schema
was the prerequisite, now built).

### 2e. Model-setup agent (geo_project) — DONE but UNPUBLICIZED
Commits `019bdad`→`66ae7a8`. New `geo_project/` package = pre-GUI versioned
Project JSON schema (provenance-tracked geometry incl. vision-draft quarantine
via blocking GEOM007, 25+ validators, builders into LE+FEM, templates, DXF/PDF
ingest, echo-back PNG renderer). Setup SUB-AGENT in `funhouse_agent/deep/`
(`build_deep_agent(enable_setup_agent=...)`, **OFF by default**), staged protocol
with hard confirmation gates. **Owner deliberately omitted this from the
presentation slide — "haven't properly tested it, don't want to publicize it."**
Treat as experimental.

### 2f. Release prep + presentation — DONE
`5cf4199` (5.1.0rc1 string + `docs/V5.1_SUMMARY.html`), `fb7831b`/`3f77718`
(presentation slide: single "Reference & review sub-agent" box, model-setup
removed per owner), `2c6e1c9` (CLAUDE.md + funhouse guide current).

### 2g. Post-release bug-hunt from a real failed session (THIS week) — DONE
The owner hit a real v5.0 failure trying to make a lateral-pile calc package.
Root-caused and fixed across three commits:
- **`c974247` — adapter ergonomics sweep.** Two root causes: (1) calc-package
  tools **re-run the analysis from raw inputs**, and `lateral_pile_analysis`
  **silently dropped an invented `E_GPa`** param → ran the 200 GPa steel default
  (0.50 mm) instead of the correct cracked-concrete result (1.86 mm; exact
  (EI)^-0.4 scaling match). (2) the deep agent's `ls`/`read_file` are a **virtual
  scratch FS** that can never see real files, so it "verified" a successful save
  as missing and looped (~850k tokens). Fix: validation on all 13 calc-package
  handlers + shared p-y builder; reject unknown top-level params; document
  `pile_E` (with steel-default warning); swept ~30 adapters for the same classes
  (raw KeyError→actionable, ~60 undocumented params documented,
  `reject_unknown_params`, file-write self-verification); deep prompt now says
  the scratch FS is not the real disk.
- **`d7a3b30` — Databricks `/Workspace` placeholder writes.** The downloaded
  calc package was the literal 11-byte string `PLACEHOLDER`: plain file I/O to
  `/Workspace` FUSE "succeeds" but isn't durably stored on many compute modes
  (binary → corrupt PDFs; DBR 14+ notebook CWD *is* /Workspace). Fix: new
  `funhouse_agent/_fileio.py` (content-verified writes w/ CRLF-tolerant
  read-back, rescue-to-`/tmp`, Databricks-aware default output dir); calc_package
  + vision `save_file` now verify content and return a `rescue_path` on failure;
  prompt + guide say save to `/tmp` or `/Volumes` then `dbutils.fs.cp`.
- **`a3d6d8e`, `fb34502` — version strings rc2, rc3** (no tag).

### 2h. Weekend QC (THIS task — see §3 for the live state)
`c374fce` (review fixes), `16a6b8f` (submodule bump), `a9f81a1` (validation
inventory), submodule `99e67b7` (reference_search fallback + gec_11 titles).

---

## 3. Weekend QC — phase-by-phase status (the work that was underway)

Owner ask (2026-06-12, paraphrased): *"QC of all code and reference retrieval,
make any necessary fixes (no wheel rebuild until Monday). Also find references
with example validation problems (FLAC manual, plus our reference manuals) and
run modules against them to check correctness. Stagger agents."*

Board: `module_work/WEEKEND_QC_2026-06-13.md`.

| Phase | Status | Evidence / commit |
|-------|--------|-------------------|
| **A. Baseline** | ✅ DONE | Full repo 7981 passed / 48 skipped; submodule 3703 passed |
| **B. Code QC** | ✅ DONE | Adversarial review of `2c6e1c9..fb34502`: 0 blockers, 4 minors + 2 nits fixed → `c374fce` |
| **C. Reference-retrieval QC** | ✅ DONE | 21 modules wired clean, 2910 figures `page_estimated=0`, recall eval off 11% / auto 44% / 0 top-1 disturbance (matches baseline); fixed submodule `99e67b7` (zero-hit OR fallback gated to auto/fill/rerank; gec_11 title normalization); pointer bumped `16a6b8f` |
| **D. Worked-example inventory** | ✅ DONE | `validation_examples/INVENTORY.md` — 25 problems across 14 modules → `a9f81a1` |
| **E. Validation runs** | ❌ **NOT STARTED** | The agent for V-001..V-008 died at the session limit with **0 tokens / 11 tool uses**. `validation_examples/RESULTS.md` and `test_published_v001_v008.py` **do not exist.** |
| **F. Wrap-up** | ⏸ this handoff stands in for it | — |

### Phase B fixes (committed `c374fce`)
From a read-only adversarial review of the week's diffs (`2c6e1c9..fb34502`).
**0 BLOCKER/MAJOR.** Fixed: `_fileio` CRLF-pair split at the 256-byte read
boundary (false-positive on correct files); `rescue_write` basename collisions
(now uniquified); latex calc-packages verified by existence/size only (the saved
`.tex` embeds figure paths and legitimately differs from the returned string);
`mse_wall` got its own alias map without `c/cohesion → c_backfill` (MSE select
fill has no cohesion input — the reject message had named a key the caller never
sent); fem2d `soil_layers[]` now require `bottom_elevation` with a clear message;
`lateral_pile_package` valid-params hint includes the metadata params.

### Phase C fixes (submodule `99e67b7`, pushed to origin/main)
`reference_search` returned 0 hits for over-constrained AND queries (e.g.
"liquefaction triggering CRR") even when individual terms hit. Ported
figure_search's OR-of-terms fallback, shared via `_query_expansion.or_fallback`;
fires **only** when the primary query (plus expansion) found nothing, and **not**
in `off` strategy (the pure-literal eval anchor). Recall eval unchanged. Also
normalized gec_11 chapter `reference_title` strings (were 4 inconsistent strings
→ duplicate inventory rows) to per-volume titles per the manifest. +1 regression
test. Deferred (logged, not done): synonym-map curation — the `liquefaction`
group has dead surface forms ("CRR" doesn't appear in the corpus).

### Phase D — the validation inventory (`validation_examples/INVENTORY.md`)
25 entries with complete inputs + published numeric answers, format `V-###`.
Spread (priority on under-validated modules): axial_pile 2, wave_equation 1,
downdrag 1, pile_group/settlement-Meyerhof 1, drilled_shaft 3, retaining_walls
(MSE) 2, seismic_geotech (M-O) 1, soe/sheet_pile 5, slope_stability 1,
lateral_pile 1 (LPILE benchmark), ground_improvement 3, bearing_capacity 1,
settlement-Hough 1, fem2d 2. 20 high-confidence, 5 medium. Extraction artifacts
(page-text dumps) in `validation_examples/extracts/`.
Couldn't extract (logged): a complete liquefaction worked example; new
reliability example beyond Duncan-2000; DM7/GEC-8 examples buried in
figures/scanned tables (need a vision/OCR pass).

---

## 4. What was UNDERWAY at the cutoff (Phase E)

**Phase E — run the 25 inventory problems against the modules as permanent
correctness checks — never started.** The plan dispatched to the dead agent
(verbatim intent, for the V-001..V-008 batch — the deep-foundations group):

1. For each entry V-001..V-008: re-verify extracted inputs/answers against the
   extract artifacts (medium/low-confidence entries especially) before trusting.
2. Convert US units to SI carefully (1 ft=0.3048 m, 1 kip=4.448 kN, 1 ksf=47.88
   kPa, 1 pcf=0.157087 kN/m³, 1 kip/ft=14.594 kN/m) — show conversions in tests.
3. Drive each module through its Python API using the method variant the example
   uses (e.g. Nordlund for V-001). Read the module `DESIGN.md` first.
4. Compare with a justified tolerance (chart-read inputs 5–10%; pure formula
   1–2%). Classify **PASS / DISCREPANCY / N/A** (genuine missing feature).
5. For DISCREPANCY: investigate extraction/units/method-variant **before**
   blaming the module. Fix real bugs surgically (read DESIGN.md + tests, run
   `pytest <module>/ -q`). Do **not** tune a module to one example over a
   legitimate modeling difference — record the delta + rationale instead.
6. Write permanent pytest tests in
   `validation_examples/test_published_v001_v008.py` (one per entry; xfail/skip
   only with a documented reason); the file must pass at the end.
7. Maintain `validation_examples/RESULTS.md` (table: entry | module | our value |
   published | delta% | verdict | notes).

**Then repeat in staggered batches** for the remaining inventory (V-009..V-017,
V-018..V-025), one agent at a time, until all 25 are run. The 5 highest-value
entries to start with (complete inputs + answers): V-001 (axial_pile Nordlund,
GEC-12 Table D-6), V-017 (lateral_pile LPILE benchmark, micropile SP-2), V-016
(soe anchored, GEC-4 Ex.1), V-009/V-010 (retaining_walls MSE, GEC-11 E4), V-004
(downdrag neutral plane, GEC-12).

---

## 5. Open decision points (owner)

1. **Wrap up vs pin?** (this handoff's purpose) — Phases A–D are done and
   durable. Phase E (validation runs) is the remaining substantive work and is
   self-contained / resumable by any model. The LE/FEM/reliability engineering is
   complete and validated; Phase E is *checking* it against more published
   problems, which could surface real bugs but is lower-risk to defer.
2. **5.1.0 final tag + PyPI publish** — still gated on the owner validating the
   rc3 wheel in Funhouse and explicitly OK'ing the tag. **A tag push
   auto-publishes — do not do this without the OK.**
3. **Model-setup agent (geo_project)** — built, OFF by default, deliberately
   unpublicized. Owner wanted to test it before promoting. Leave as-is.

---

## 6. How to resume (concrete)

**Environment (unchanged):**
- Worktree: `C:\Users\socon\OneDrive\dev\GeotechStaffEngineer\.claude\worktrees\v5.1-todos`
  (work here, not the main checkout). It's a git worktree on branch
  `worktree-v5.1-todos`; merge to master with `git merge --ff-only` from the main
  checkout, as every milestone has.
- **Venv python (system `python` has no pytest):**
  `C:\Users\socon\OneDrive\dev\GeotechStaffEngineer\.venv\Scripts\python.exe`
- Git identity for commits: `-c user.name=soconnell345 -c user.email=soconnell345@gmail.com`
- End commit messages with the `Co-Authored-By` trailer.

**To finish Phase E:** spawn one staggered agent per inventory batch (V-001..008,
then 009..017, then 018..025) using the §4 method. Each writes
`validation_examples/test_published_v###.py` + appends to
`validation_examples/RESULTS.md`. Commit per batch; merge to master at the end of
the weekend-QC workstream. Update `module_work/WEEKEND_QC_2026-06-13.md` Phase E
checkbox and the `## Log`.

**To pin instead:** nothing to do — everything is committed and pushed. This file
+ the QC board + the inventory fully capture the state. Resume by reading them.

---

## 7. Standing constraints & gotchas (carry forward — important)

- **No version bump / tag / publish without explicit owner OK.** A `v*` tag push
  auto-publishes to PyPI. rc strings are committed without tags on purpose.
- **`ANTHROPIC_API_KEY`** is read from the Windows *User* env at runtime — never
  pass it through chat/transcripts. Live tests are opt-in (`RUN_LIVE_TESTS=1`).
- **Owner is not a developer** — skip git/dev-procedure rationale; report
  actionable results. Works in big batches (~weekly), prefers autonomous
  milestone-level operation, not per-step confirmation.
- **Stagger subagents** — the session keeps hitting usage limits; run one agent
  at a time, commit at milestones so a cutoff is cheap (this is why the work is
  in phase-committed chunks).
- **Databricks `/Workspace` writes are unreliable** (the placeholder bug, §2g) —
  outputs go to `/tmp` or `/Volumes`. Already handled in code as of `d7a3b30`.
- **Edit/Write tools are pinned to this worktree** — to change a file in the main
  checkout, author here + `cp`, or use a Python heredoc via Bash.
- **geotech-references is an editable install** (`pip install -e`) resolving to
  the **main checkout's** submodule, while `funhouse_agent` resolves to this
  worktree. After a submodule pointer change, `git submodule update` the main
  checkout (done this session; both now at `99e67b7`).
- **`np.trapezoid`** (not `np.trapz`) — numpy ≥ 2.0. SI units throughout.

---

## 8. Known tracked future work (recorded, not assigned)

- SRM mesh-consistency study (`fem2d/UPGRADE_PLAN.md`; `nr_method`/`nr_fallback`
  knobs exist but the opt-in tangent path is **not validated**).
- Incompressible undrained elements (B-bar / T15) for fem2d.
- Rapid-drawdown 3-stage (designed stub raising `NotImplementedError`).
- ipywidgets confirmation-form renderer for the setup agent (payload final,
  rendering only).
- Reliability increments: random-field FEM via gstools, system reliability, LRFD
  calibration.
- References v5.2: synonym-map curation (the deferred Phase C item), Tier-1
  lookups, larger gold set, keep-`auto`-default-vs-opt-in decision.
- Phase E validation runs (this handoff's main loose end).

---

## 9. File map (where the important things live)

- `module_work/WEEKEND_QC_2026-06-13.md` — weekend QC board (phase status + log).
- `validation_examples/INVENTORY.md` — 25 worked-example validation problems.
- `validation_examples/extracts/` — page-text dumps used to extract them.
- `validation_examples/{scan_examples,scan_pdfs,find_kw,dump_pages}.py` — the
  harvesting helper scripts.
- `funhouse_agent/_fileio.py` — verified-write + rescue helpers (placeholder fix).
- `funhouse_agent/adapters/calc_package.py` — 13 calc-package handlers.
- `funhouse_agent/adapters/lateral_pile.py` — the adapter at the center of §2g.
- `reliability/`, `geo_project/`, `slope_stability/`, `fem2d/` — each has
  `DESIGN.md` + `UPGRADE_PLAN.md` + `VALIDATION.md`.
- `docs/V5.1_SUMMARY.html`, `docs/agent_workflow_slide.{html,png}` — owner-facing.
- `docs/funhouse_agent_guide.md` — install + Databricks gotchas section.
- `CLAUDE.md` — project instructions, current as of `2c6e1c9`.
- Test wheel: `C:\Users\socon\OneDrive\dev\v5_test_wheel\geotech_staff_engineer-5.1.0rc3-py3-none-any.whl`
